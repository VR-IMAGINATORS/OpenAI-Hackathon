Repair only the supplied candidate, following the current mechanical failures and verified findings. Return a complete revised candidate with the supplied next revision and exact unchanged contract digest. Never modify the frozen contract, initial conditions, goals, difficulty or catalog to make a failure disappear. Narrative explanations are also repairable, but cannot retroactively invent initial props. Preserve step semantics: send costs one photo send; use/place/retrieve/move each one action. Ensure held/placed/unavailable and usable/spent/broken transitions, retrieval, body restrictions, fact effects and total time are consistent. Subsequent evaluation is independent and receives none of this repair history. If the fixed contract prevents a valid solution, do not conceal that by silently changing it.

The state-machine meaning of move is crossing to a DIFFERENT declared locationId, with itemIds:[] and itemEffects:[]. Held items follow automatically; do not repeat their location changes in itemEffects. Standing up or repositioning inside the SAME location is a barehand use step (empty itemIds and itemEffects), with any declared posture factEffects. Describe actions concisely, ideally under 200 Japanese characters per step, citing named fixed props instead of repeating all their measurements. Retain essential receiving/placement/carry and action distinctions.

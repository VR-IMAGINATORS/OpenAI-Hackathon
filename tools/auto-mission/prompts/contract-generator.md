Generate only the model-owned part of a fixed mission contract. Invent new, everyday-object-solvable, sequential physical escape gimmicks. Code supplies the immutable input digest, world, difficulty and object catalog; do not duplicate or change them. Declare every location, fact and allowed fact value, initial player location, body condition, stage props, and ordered obstacle goal necessary to understand physical feasibility. Objects to be materialized come from the supplied catalog and are not initially stage props. Conditions are AND equality checks on declared facts. Each obstacle must be unsolved initially and achievable before the next. Escape conditions must represent actual escape. Leave room for multiple ordinary-object solutions but provide enough physical detail to avoid assumptions. Use 2–5 obstacles as configured. Do not include a candidate story or silently relax limits.
The contract schema stores body freedom/reach/limitations in initialState.description and obstacle constraints, stage props in initialState.props (id, description, locationId), and state values in factDefinitions (key, allowedValues, initialValue). Declare state keys for every physical condition later changed by a witness. Include precise prop geometry and positions that make the intended ordinary tools feasible; do not leave solvability to a later story inventing measurements or access paths. IDs must match the schema pattern and be unique.

Keep the contract concise: prefer a small set of necessary facts and props (roughly 8 facts and 6 props), with clear physical margins. Avoid unnecessary engineering tolerances or exact thermal calculations. Invent fresh mechanisms that demonstrably fit the explicit ordinary object properties; do not design a narrow opening first and then assume a tool fits. Do not assume capacity, geometry or strength beyond the catalog. Keep prose compact while retaining all facts needed to establish feasibility.

CRITICAL terminal invariant: EVERY obstacle goalCondition and escapeCondition must still hold simultaneously at the END. Never put player_location=intermediate_room (or another temporary state) in an intermediate goal. Use persistent resolved-state facts (latch=released, restraint=cut) or declared achievement flags for milestones; the final escape goal may require player_location=outside. The same fact key must never demand two different goal values across the contract. Changing an earlier required goal later makes the contract impossible and is rejected before story generation. Reaching the next room can be a step precondition or narrative constraint without being a permanent terminal goal.

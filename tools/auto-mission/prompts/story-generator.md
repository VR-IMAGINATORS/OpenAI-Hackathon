Generate one candidate under the frozen contract, preserving its digest and supplied revision. Tell an opening, sequential obstacles and ending plus sample solutions and one complete executable witness. Examples do not forbid alternative solutions. Every item must be in the catalog and starts unmaterialized; its initialPlacement cannot invent a free prop. Steps use only send/use/place/retrieve/move. Send materializes at most maxPhotosPerSend items and costs one photo send. Every use/place/retrieve/move costs one action; do not bundle independent operations to hide costs. Steps declare every precondition, declared-fact from/to change, item state effect, duration and rationale. All emitted fields are required; use null only where allowed. Held items move with the player; placed items remain and require retrieval; spent/broken/unavailable items cannot be revived or used. State location, reach, body freedom, receiving materialized items and tool carry explicitly. Complete each goal before progressing and all escape conditions at the end. Do not edit world, initial facts, props, goals or limits. Separate game duration estimates from API latency. Match actual schema field names precisely.
Fact preconditions are {key,value}; effects are {key,from,to}. Item effects are {itemId,from:{phase,locationId,condition},to:{phase,locationId,condition}}. Before send, item state is {phase:"unmaterialized",locationId:null,condition:"usable"}. Never use null for a placed/held item's location. ItemInstance.initialPlacement is explanatory text describing the receiving position, not an initially existing object. Every item reference is an ItemInstance.id, not catalogId. Stage props are contract.initialState.props and cannot be referenced as materialized items. Use the provided JSON schema exactly.

For a barehand interaction with an already declared stage prop (e.g. turning a tap or opening a door), use kind use with itemIds:[] and itemEffects:[], and explicit declared factEffects. This still costs one action and must be physically possible. Do not invent a materialized item ID for stage props.

The state-machine meaning of move is crossing to a DIFFERENT declared locationId, with itemIds:[] and itemEffects:[]. Held items follow automatically; do not repeat their location changes in itemEffects. Standing up or repositioning inside the SAME location is a barehand use step (empty itemIds and itemEffects), with any declared posture factEffects. Describe actions concisely, ideally under 200 Japanese characters per step, citing named fixed props instead of repeating all their measurements. Retain essential receiving/placement/carry and action distinctions.

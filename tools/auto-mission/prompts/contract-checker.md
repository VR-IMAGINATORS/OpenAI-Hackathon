Independently inspect the supplied contract before story generation. Check that fixed world/rules/catalog are retained, initial bodily restrictions and reachability are explicit, goals and state definitions are coherent, and a physically feasible sequence is possible within photo/action/time limits. Do not repair or reinterpret the contract and do not assume hidden resources. Return pass only when these feasibility checks are established; necessary missing information means fail. Give concise reasons and exact references. This is an independent call, not continuation of generation.
For this contract-only check, evidence targetPath is a JSON Pointer into the supplied contract (there is no candidate yet), and finding.role is contractChecker. Give each finding a provisional unique id; the harness owns stable IDs. Check the geometry/placement of initialState.props and explicit body/reach facts. The story generator cannot add missing initial affordances after this check passes.

CRITICAL terminal invariant: EVERY obstacle goalCondition and escapeCondition must still hold simultaneously at the END. Never put player_location=intermediate_room (or another temporary state) in an intermediate goal. Use persistent resolved-state facts (latch=released, restraint=cut) or declared achievement flags for milestones; the final escape goal may require player_location=outside. The same fact key must never demand two different goal values across the contract. Changing an earlier required goal later makes the contract impossible and is rejected before story generation. Reaching the next room can be a step precondition or narrative constraint without being a permanent terminal goal.

> 状態: 後続のユーザー判断により画像生成部分は 2026-09-12-call-to-past-flare-preflight-runtime.md で置換。ゲーム状態機械とH3の判断は維持する。

# Call to the Past: nativeツールの採用

ユーザー回答により、初回はCodex内蔵ツールを優先する。画像生成の公開引数にモデル指定がないためFlare指定を保証できず、native imagegenと記録する。判定はSol high subagentを指定し、画像認識と攻略判定を分離する。直接OpenAI APIへ自動切替する実装は追加しない。

ゲーム規則はPythonの状態機械、自由な写真解釈・創意解法・会話・画像はCodexが担当する。外部送信やメディアの成否がゲーム回数を二重消費しないよう、状態確定とメディア作成を別段階にする。画像・動画は通常必須だが利用者が明示選択すれば文章版へ進め、実生成達成フラグは未達のまま残す。

H3は既存h3-videoへの依存とし、素材・プロンプト・条件・見積をハッシュで承認へ結び付ける。原本写真をH3へ直接送らず、作成した動画用フレームのみを対象にする。送信開始後は同じ枠を再利用せず、request IDまたは要確認の状態を保持する。

6シーンごとに3本の攻略順を用意。各障害に前段の道具へ依存しない基準解を持たせ、全18順序を3行動で成立させる。基準解の物性と入力枚数はテストするが、現実の物理シミュレーションや全創意解法の網羅は主張しない。

# エンディングタグの確認記録

2026-09-14 / `codex/ending-tags`。ローカル実装。共有作業ディレクトリに同時進行の動画MP4関連変更も存在するため、全体テスト件数は検証時の作業ツリーに対する件数。

## 実装

- 固定40タグ、日英表示名、AI用判定条件。
- 文章用の生成1回でタグと短文を返し、保存後に映像指示を別の1回で生成する。全確定行動を対象にし、タグの出典は映像用の直近2行動に限定しない。
- 文章を画像制作前に保存。storyStatusを公開し、動画未完成・無効でも文章を自動表示。
- 動画無効・素材なし・動画予算不足では文章のみ生成。Responses予算・1回制限を継承し、mockと中断は実生成なし。
- 文章のみの実行枠を動画枠から分離。画像・動画失敗でも保存済み文章を維持。

## 実施した確認

| 確認 | 結果 |
| --- | --- |
| `npm.cmd run check` | 型、標準シナリオJSON、18構成、ゲームコア設定を確認し成功 |
| `npm.cmd test` | 最終実行358件成功、失敗0、skip0 |
| `npm.cmd run build` | check、Vite、サーバーTypeScriptビルド成功 |
| `tests/ending-tags.test.ts` | 40定義と日英名、0行動・失敗行動、同一道具、組み合わせ、手ぶら、学習、破損済み再利用の条件、根拠非公開 |
| `tests/ending-pipeline.test.ts` | 40タグ・全行動の入力、序盤のタグ根拠、動画なしの共通契約、不明タグ・架空/重複ID・長文の拒否 |
| `tests/ending-diagnostics.test.ts` | 既定producerの最初の画像API呼び出し時点ですでに文章公開済み、完成画像なしでも文章が残る |
| `tests/ending-jobs.test.ts` | 文章公開後の画像失敗、動画無効、再読込相当の再enqueue、キャンセル、動画予算不足、動画枠使用中の文章生成 |
| `tests/ending-http.test.ts` | 認証付きHTTPで動画送信前にstoryStatus=readyと本文を取得、既存の所有者分離・再読込を維持 |
| `apps/web/tests/ending-smoke.cjs` | Chrome headless 390×844で動画完成前のタグ・短文自動表示、文章→動画の位置関係、幅収まり、動画無効時の文章poll、再読込・日英・認証失効・期限切れ |

ブラウザ確認は既存ローカルPlaywrightを利用し、Viteを127.0.0.1:5178で起動。API・メディアはfake応答であり、実音声・実AI・実動画デコードの成功を示すものではない。

表示確認画像: `artifacts/ending-tags-waiting-mobile.png`。架空のAPI応答によるレイアウト確認画像。

## 未実施

- 実AIでの40タグの分類精度・短文の面白さ・所要時間の評価。
- 実音声入力・実機写真・スマホ実機での通し試遊。
- 実画像・動画生成と映像内容の整合評価。
- 公開環境へのデプロイ、push、マルチレビュー。

liveでは動画無効時にも文章生成分のAPI利用が追加される。終了直後は既存の最大12秒の音声文字起こし猶予とAI生成待ちがあり、文章が届いた時点から約2秒間隔の取得で自動表示する。

## 同日の失敗報告に対する修正

ユーザーから「動画生成に失敗しタグが表示されなかった」と報告された。実プレイのエラーコード・実行環境は未確認。コードでは、e7ab1ccのcreateEndingDesignが文章と映像脚本を一緒に生成・検証しており、映像側の失敗で文章を保存できない経路が残っていた。また動画有効プレイの文章生成は動画の実行枠待ちに含まれていた。

修正：全プレイを文章フェーズ→保存・配信→動画フェーズとする。story/directionは別のAPI応答・別の1回上限。動画の枠や映像応答の成功を文章公開の条件にしない。UIは保存済み文章を引き続き自動表示する。

- 最終 `npm.cmd test`: 362件成功、失敗0、skip0。
- `npm.cmd run build`: 型・JSON確認、UI・サーバービルド成功。
- 既定producerの回帰テストで、映像API失敗・不正な行動ID・不完全な映像応答の各ケースでもstoryStatus=ready、非nullタグ・本文が残ることを確認。
- 動画有効の次プレイが、前プレイの動画枠使用中でも文章を公開すること、動画待機中の期限切れでも文章が残ることを確認。
- 認証付きHTTPで文章公開後に映像準備へ進む順序を確認。
- Chrome headless 390×844で同じプレイの映像脚本エラー後にもタグと本文が表示され、再読込でも残ることを確認。API・メディアはfake。
- localhost:4310/healthzはversion=localで応答したが、起動中サーバーの再起動や公開環境への反映は実施していない。実プレイの障害解消・実AI品質は未検証。

初回確認の不足：文章を事前注入した画像失敗テストだけでは、文章と映像脚本を同じAI応答にした依存を検出できなかった。先行表示の確認は、保存より前の全工程と待機枠を含める。

## 文章失敗時の動画継続・解除0個（2026-09-14）

「タグ・結末文を作る段階で失敗」と表示され動画が作れないとの追加報告。0334524ではrunStoryの失敗がジョブ全体を終了させていた。文章側のエラーコード・環境・発生時刻は未提供で、実プレイのAI失敗原因は未確定。解除0個を拒否する条件はない。

- 文章失敗をstoryStatus/storyErrorCodeに保存して動画フェーズへ進む。文章なしの映像は確定結果・行動・状態から制作。文章フェーズだけの60秒タイムアウトを追加し、全体キャンセルと区別。
- EndingDesign/PreparedEndingから文章の必須依存を削除。文章失敗時は抽出の再試行をせず、任意の伏線なしで動画を作る。失敗した文章を正常なAI生成物として表示しない。
- 課金なしテストで0行動・全試行失敗・解除0個、失敗試行への非nullタグ、文章通信失敗・不正タグ・不正応答・未完了応答・証拠上限、文章タイムアウト、動画と文章の両エラー保持、全体キャンセルを確認。
- `npm.cmd test`: 383件成功、失敗0、skip0。共有作業ツリーで実施。
- `npm.cmd run build`: 型・JSON・UI・サーバービルド成功。
- Chrome headless 390×844: 文章失敗中も動画poll継続、動画完成・再読込後も文章エラーとPlay IDを表示、両エラー表示を確認。API・メディアはfake。
- 運用ログの解除数・試行数・失敗試行数、スキーマの項目名だけを使う検証詳細の記録を確認。応答本文・入力・キー・写真は記録しない。
- 調査手順は `docs/ending-diagnostics.md`。既存サーバーの再起動、実APIでの再現、公開環境への反映は行っていない。

ユーザー確認：「タグちゃんと出てたよ」と表示成功の報告あり。実行環境・適用版は未確認で、文章失敗時の動画継続を実環境で確認した報告とは区別する。

## 出典ID不正の報告に対する修正

画像でPlay ID `7e52d52a-3b35-4c14-8656-c510bddc41e2`、解除1個、ENDING_STORY_INVALID_SOURCESを確認。文章の出典IDが照合に失敗した経路と特定。画像だけでは実際の不一致IDは分からず、行動ID等の取り違えは再現テストで確認した原因候補。

- 文章/動画/抽出それぞれの参照可能IDをschemaのenumにする。出典ID・行動ID・eventIdの区別と空出典の扱いを明示。抽出で提示されなかった出典も採用しない。
- 初回の出力不正・未完了に限って文章を1回再生成し、出典・タグ根拠・長さを再検証。抽出済み証拠を再利用。拒否・通信・認証・予算エラーは再試行せず、全体60秒・共通予算・キャンセルを維持。
- 出典混同・架空ID・タグ根拠不正・文字数超過・不正JSON・未完了からの回復、0行動/0証拠、抽出の重複実行防止を検証。解除1個で出典エラー→修正成功→文章公開→動画完成を既定producerで確認。
- 修正開始ログと、不正出典/行動ID混同/eventId混同の件数を追加。実ID・本文・認証情報は記録しない。
- `npm.cmd test`: 418件成功、失敗0、skip0。`npm.cmd run build`: 型・標準JSON・UI・サーバービルド成功。
- すべて課金なしの模擬API。今回UI変更はなくブラウザsmokeの再実行は省略。稼働サーバーの再起動・公開環境反映・実APIでの回復成功は未実施。

## INVALID_RESPONSE / response の追加報告

ユーザーからPlay ID `3e8190fe-5ed3-421a-873e-03409d5f9bd0` のログを受領。解除1個・行動1回・失敗0回、ENDING_STORY_INVALID_RESPONSE、validationFields=response。タグ不正か送信前失敗かを旧ログでは区別できなかった。

合成入力の140出典で、証拠34,053 bytesに対しschema19,364 bytesとなり、16 KiB上限でAI送信回数0のまま同一ログになる経路を再現した。当該実プレイの入力サイズはなく、実原因との同一性は未確定。

- 長いID一覧をリクエスト限定の短い参照へ変換。証拠の削減・上限緩和は行わず、AI返答を元IDへ復元する。
- 大量出典・多数行動の文章/タグ/映像生成、抽出チャンクでの引用と元IDの対応、未知参照・非提示の元IDを拒否するケースを確認。リクエスト検証を通してからfake AI応答を返すテストにした。
- 送信前の失敗はENDING_STORY_INVALID_REQUEST / request_schema等として出力。上限超過時にAPI試行を消費しないこと、詳細から値を漏らさないことを確認。
- `npm.cmd test`: 422件成功、失敗0、skip0。`npm.cmd run build`: 型・標準JSON・UI・サーバービルド成功。
- 模擬APIのみ。UI変更はなく、ブラウザsmoke再実行・実API・稼働環境への反映は未実施。

前回の検証不足：fakeAiがリクエストサイズの検証をせず応答を返していたため、enum化で増えたschemaサイズを検出できなかった。

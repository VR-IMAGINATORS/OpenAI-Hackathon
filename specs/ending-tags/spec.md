# エンディングタグと先行リザルト

Status: approved direction (2026-09-14)

ユーザーの「そのタグ案で進めよう」「さらに倍」「動画生成を待っている間に読んで楽しむ」を反映。既存提案の実装方針を具体化する。タグ20案は40案へ拡張する。

- T01: 固定40タグからAIが主役1個または該当なしを選ぶ。表示名は日英固定、根拠は全確定行動から選ぶ。成功を必須にしない。写真送信のみ・未確定の会話は対象外。
- T02: 専用の文章生成呼び出しでタグと短い結末文（日本語40〜80字目安、最大240文字）を先に生成・保存する。映像脚本の生成・検証と同じ応答にしない。結末・解除数はサーバー確定値を維持する。タグに沿った道具・行動・結果を短く描き、未発生の成功・救助・死亡を追加しない。
- T03: 表示順は結末→解除数→タグ→短文→動画。文章生成・検証後、画像制作前に保存・配信し、操作や動画再生を待たず自動表示する。長い評価文の重複表示は取り除く。
- T04: 文章と動画の状態を分ける。画像・動画失敗でも保存済みの文章を維持。動画無効・素材なし・動画予算不足でもliveモードなら文章のみ生成可能。mockと中断は実生成しない。
- T05: 文章は既存Responsesの全体予算・同時数を継承。storyは通常1回、出力検証失敗・未完了応答の修正時のみ追加1回（最大2回、同じ60秒内）。通信・認証・予算エラーや拒否は再試行しない。動画有効時は別途directionを最大1回呼ぶ。全プレイの文章生成枠を動画の枠から分離し、動画の待ち行列・受理不明の処理に文章を待たせない。有限キュー・期限・キャンセル・所有者認証を維持する。
- T06: AI出力のタグID、根拠行動ID、重複、長さ、機械的に確認できる条件を検証する。分類の意味はAIが判断し、出典IDの実在だけで正しさを保証しない。
- T07: 未該当はタグ欄を省略。生成中・失敗・無効を区別する。失敗時は必要なら最後の行動文をその旨を付けて表示。再読込で再生成しない。
- T08: 文章・タグのAPI失敗、応答不正、根拠不正、証拠抽出失敗でも動画制作は続ける。文章なしの映像は確定した結末・行動・状態を根拠にし、文章・タグや失われた伏線を捏造しない。文章側は最大60秒で打ち切り、全体キャンセル・期限・共通予算は維持する。元画像や映像API自体の失敗まで成功を保証するものではない。
- T09: 解除0個・全試行失敗・0行動を正常な結末として扱う。失敗した確定行動にも適合するタグを選択可能。0行動や根拠不足はtag=null。storyErrorCodeを動画errorCodeと別に保持し、動画完成後もPlay IDとともに調査可能にする。
- T10: 出典ID・タグ根拠行動IDを、その応答に実際に提示したIDのenumで制限し、両者を混同させない。空の証拠はusedEvidenceIds=[]、0行動はtag=null。修正時も不正IDを黙って削除して本文を採用せず、証拠抽出を再実行せずに文章を再生成・再検証する。動画の出典と行動ID、証拠抽出のsourceIdにも同じ候補制限を適用する。
- T11: enumのID一覧が4 KiB超またはID長が200文字超なら、AIリクエスト内だけの短い参照へ変換する。証拠・行動は保持し、返答を元IDへ復元して検証。送信前の検証失敗（INVALID_REQUEST）とAI返答不正（INVALID_RESPONSE）を別コードにする。schema16 KiB・入力128 KiB上限は維持する。
- T12: 手がかり抽出の引用不一致・出典不正・形式不正・未完了・入力上限・抽出専用期限切れは、文章生成の必須成功条件にしない。抽出は全チャンク合計15秒以内とし、不正な抽出結果を丸ごと破棄して、サーバーに保存した元の提示記録を最大32 KiBまで使って文章を生成する。文の途中で切らず、開始時の説明・確定結果を優先し、序盤と終盤の記録を保持する。省略はevidenceIncompleteで文章・映像へ伝え、欠けた伏線を補作しない。抽出を再実行しない。拒否・通信・認証・共通予算・全体キャンセルは迂回しない。T08の動画継続は、文章の回復も失敗した場合に適用する。
- T13: 抽出結果の合計も最大48 KiBとし、確定行動等を含む入力全体に修正指示の余地を残す。入力が大きい場合は各行動の重複した全状態を変更箇所へ圧縮し、全行動・実際の状態変化・最終状態は維持する。タグ候補から機械的に成立しない条件を事前除外し、タグの出力検証に失敗したときは既存の1回の修正枠でtag=nullをschemaに指定して本文全体を再生成する。不正タグだけを取り除いて旧本文を採用しない。

検証: 課金なしで40定義、出力検証、全行動入力、文章の画像制作前公開、画像失敗時保持、動画無効、mock・中断、再読込、モバイル自動表示を確認。実AI文章の面白さ・実動画品質は別の試遊項目。

本仕様のT03は `specs/ending-video/spec.md` E08と `specs/story-integration/spec.md` の動画後本文表示を更新する。

同日の不具合報告を受けた修正：従来はタグと映像脚本を同時生成し、映像脚本の失敗でタグも未保存になる経路があった。ユーザーの「先に表示」の要求に合わせT02/T05を更新。実プレイで発生したエラーコード自体は未確認。

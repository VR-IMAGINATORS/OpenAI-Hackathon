# 検証記録 — 2026-09-18

## 自動確認

- 全体テスト: 987件成功、失敗0件（終了処理の追加修正前）。
- 終了処理の失敗を再試行不可にする修正と回帰テスト追加後: Codex関連20件成功、失敗0件。
- TypeScript `--noEmit`: 最終変更後も成功。
- シナリオ/ゲーム設定検証、Vite UIビルド、サーバーTypeScriptビルド: 成功。
- `git diff --check`: 成功。既存Windows改行に関する警告のみ。
- 実際の対応Codex実行ファイルを使用し、startWorkerのstdio初期化とcloseを確認。ログイン・推論は実行していない。

テストは命令・スキーマ・写真変換、連続判断の通知/写真解放、取消と停止確認、開始応答不明/枠切れ、終了処理失敗、APIへの自動fallbackがないこと、既存予算制限の維持を検証する。実モデルの判断品質を保証するテストではない。

## 実機確認待ち

追記: ユーザーが旧端末認証版でログイン、写真送信からギミッククリア、および `Codex判断: completed / 9801ms` を確認した。接続→実ゲーム判断は成功。音声全体や終了処理の項目をすべて確認したという報告ではない。後続の画面認証版は `../codex-player-login/verification.md` に分離する。

本統合でのChatGPTログイン、写真判断、音声往復、連続プレイ、終了/再起動はユーザー実機確認待ち。単体PoCでユーザーが確認済みの認証・写真判断は `../codex-login-poc/verification.md` に分離して記録している。

今回はローカル本人用。公開運用、複数ユーザーの認証分離、スマホ接続、プレイヤーのBYOK入力は未実装。App Serverの出力token上限はAPIと同等ではない。音声・画像・動画等のAPI課金経路は残る。

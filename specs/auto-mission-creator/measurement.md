# 初回実測記録

2026-09-15。通常のnpm testは模擬通信であり、以下は--liveによる別計測。単発の試行で、代表値・p95・所要時間保証ではない。

| 試行 | 設定/対象                       | 結果                      | 実経過   | 観測                                                                  |
| ---- | ------------------------------- | ------------------------- | -------- | --------------------------------------------------------------------- |
| 1    | Astra high・生成・個別上限180秒 | API_TIMEOUT               | 180.04秒 | 固定条件生成中に停止。使用量不明、0扱いしない。                       |
| 2    | Astra high・生成・個別上限600秒 | OUTPUT_INVALID/incomplete | 293.88秒 | 固定条件生成で出力8,000token上限、入力1,286token。候補本文なし。      |
| 3    | 初期正常fixtureの実評価         | REVIEW_INCOMPLETE         | 80.83秒  | 3評価並列23.76秒、Astra指摘検証57.01秒。fixtureの物性説明不足を検出。 |

初期正常fixtureはスプーンの形状やタオルの断熱性を明記していなかった。検査を緩めず、普通の道具の寸法を明示し、曖昧な熱障害を常温の濡れた取っ手の吸水へ差し替えた。元の試行を成功へ書き換えていない。

個別タイムアウトを600秒と全体残り時間の小さい方へ変更。Astraのモデルは維持し、生成/修正のreasoningEffortをmediumにした比較設定で固定条件生成に成功（149.93秒）。独立検証99.12秒で、ペンの寸法・コップの容量不足によりCONTRACT_INVALIDとなった。日用品の寸法/容量を明記し、生成mediumを初期設定に反映して追試。開発マルチレビューはユーザー指示により未実施。

## 評価器の実API確認

| ケース                         | 結果           | 実経過  | API回数       |
| ------------------------------ | -------------- | ------- | ------------- |
| 条件を明記した正常例           | passed         | 14.61秒 | 4             |
| 行動回数不足                   | STORY_REJECTED | 0.01秒  | 0（機械検査） |
| 使用不能道具の再利用           | STORY_REJECTED | 0.01秒  | 0（機械検査） |
| 両腕・指が拘束された状態で操作 | STORY_REJECTED | 68.65秒 | 4             |
| 具現化場所・受け取り説明不足   | STORY_REJECTED | 67.22秒 | 4             |

上表はケース別の最終確認結果で、単一の連続ベンチマーク実行の成績ではない。最初の拘束例ではLLMの指摘箇所の配列番号がずれ、OUTPUT_INVALIDで停止した（誤合格はしていない）。コードで生成した引用文/JSON Pointer一覧を共通入力へ追加し、推測で番号を数えない指示で再検証した。元の失敗履歴もruns/に保存している。

## 生成本文を取得した試行

寸法/容量明示後のAstra mediumでは、固定条件生成114.90秒、固定条件検証75.08秒、本文生成152.22秒で、3ギミック・21手順の候補を取得した（合計342.24秒）。機械検査でmoveの表現を指摘し、修正入力が48KiBを超えBUDGET_EXCEEDEDで停止した。保存案からの実データ回帰で原因を再現し、成功チェック/全状態履歴をAPI入力から省略（保存結果には維持）。必要な失敗・最後の状態・全文候補・固定条件は保持した。

同一location内の立ち上がり/位置調整は素手のuse、moveは異なるlocationへの遷移、保持道具は自動追従することをプロンプトに明記。次の試行を最新設定で実行中。

## 終端条件の相互矛盾

次の試行は本文を取得し、修正入力も上限内で送信できたが、複数のギミックが同じplayer_locationに異なる部屋を終端条件として要求していた。これは固定条件のまま修正できないため、2候補の検査後に試行を手動停止。保存済みJSONは停止前のチェックポイントであり完了記録ではない。コードで全ギミックと脱出条件の同一key/異valueを契約固定前に拒否する検査を追加。合格条件は緩めず、中間の目標を永続的な達成フラグで表す指示へ変更した。

## 最新設定での完走（2026-09-15）

生成から最終判定まで **356.59秒（約5分57秒）**、7 API呼び出し、修正0回でpassed。生成案は「三つの掛け金を順に解く」。写真送信3回/写真3枚/20行動、構造化手順の見積り141秒。

| 段階                         | 実測     |
| ---------------------------- | -------- |
| Astra 固定条件生成（medium） | 92.26秒  |
| Astra 固定条件検証（high）   | 75.31秒  |
| Astra 本文生成（medium）     | 146.51秒 |
| 独立3評価の並列区間          | 19.56秒  |
| Astra 指摘検証（high）       | 22.90秒  |

生成前半の3呼び出しが約314秒（全体の約88%）を占める。2〜3分の目標は未達。10分上限内で完走する基準構成を確認できたので、次の短縮案はこの基準と比較できる。これは多数の改修試行後の単発成功であり、成功率や所要時間の保証ではない。失敗した試行も上記に残している。

非blocking指摘1件: endingの時間表記152秒と構造化手順の合計141秒が不一致。評価者と検証役が確認しており、どちらも300秒以内で成立性を阻害しないため、軽微な表記不整合として残して合格となった。生成案を手作業で合格へ書き換えていない。

既知usageは入力55,004/出力18,228 token。2026-09-14確認の標準単価による参考費用は約1.07 USD（キャッシュ割引等は未反映、請求額の保証ではない）。

## 最終の開発検証

- npm.cmd test: 476/476成功。
- npm.cmd run build: 型検査・シナリオ検査・Web/Serverビルド成功。
- CLI mock生成/再評価/HTML再描画、正常/異常fixture、実API生成とケース別評価を確認。
- HTMLはChrome headlessで表示確認。開発マルチレビューはユーザー指示により未実施。

# M2: 状態シミュレーション・最終判定

Status: completed (2026-09-15)

参照: [計画](../plan.md)、[データモデル](../data-model.md)、[仕様](../spec.md)。仕様の詳細は参照先を正とする。

## 作業チェックポイント

- [x] 計画のM2対象を実装
- [x] 対象の自動テスト/型検査を実行して結果を記録

## 完了条件

plan.md §6/§8のM2責務・検証を満たす。前提矛盾、範囲超過、達成不能があれば無断再設計せず報告する。実API検証は模擬検証と区別する。

## 検証実績

`npx tsx --test tests/auto-mission-simulate.test.ts`: 10件合格。正常例・機械破綻2例・AI評価対象2例をJSON fixture化。実APIの判定品質は未検証。`npx tsc --noEmit`: 担当モジュールの型エラーなし、並行実装中のpipeline.tsにPhaseTiming status型のエラー1件があり全体合格は未確認。

内部API: `deriveVerdict(candidate, mechanical, reviews, verification)`。候補のJSON Pointerと引用を検証するためcandidateを渡す。`validateEvidence(root, finding)`と`resolveEvidence(root, path)`はコントラクト評価にも利用可能。

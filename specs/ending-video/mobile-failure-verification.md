# モバイル試遊のエンディング失敗への対応

2026-09-14。作業ブランチはユーザー指定の `feature/shibuya/integrate-mock-features`。

## 調査で確認したこと

- 報告は未デプロイの `npm.cmd run play:mobile` で発生した。従来の表示・ログでは制作段階と原因を区別していなかったため、報告されたプレイの失敗原因自体は確定できない。
- ローカル設定は動画生成が有効で、falキーも設定済みだった。秘密値は表示・記録していない。動画回数上限は1回だったため、合意済みの100回へ `.env.local` の該当値だけを変更した。この私有設定はコミットに含めない。
- GitHubのSecret・Variablesはローカル試遊へ自動反映されない。起動時に有効状態・実際の回数上限・ローカル設定を使うことを表示するようにした。

## 修正

- 直近2行動のうち最後の1行動だけを映像化するとき、以前は先の行動に対応する画像で連続性を検査していた。このため正しい参照画像があっても制作に失敗し得た。利用可能な行動前画像の一覧を脚本生成へ渡し、採用した最初の行動のバージョンに一致する画像を編集に使う。各API要求の添付画像は最大2枚を維持する。
- 物語、画像生成、画像検査、fal送信、取得、保存の失敗を固定コードで区別する。上流サービスの応答本文・キー・画像・会話は公開表示にも診断ログにも含めない。
- プレイヤーには失敗の説明を表示し、「不具合報告用の情報」でコードを確認できるようにした。既存プレイの再送や自動再試行は追加しない。

## 課金なしの検証

| 確認 | 結果 |
| --- | --- |
| `npm.cmd run check` | 成功 |
| `npm.cmd test` | 325件成功、失敗・skipなし |
| `npm.cmd run build` | 成功 |
| `npm run format:check` | 成功。指定のPrettier 3.6.2をnpm exec経由で使用 |
| `git diff --check` | 成功 |
| `apps/web/tests/ending-smoke.cjs` | Chrome headless・390×844で成功。失敗理由と報告コード、既存の再読込・表示・認証・期限切れを確認 |

`tests/ending-diagnostics.test.ts` は実際のEndingJobs制作処理を使い、最後の1行動だけが選ばれた場合の参照画像選択から動画保持まで確認する。AI通信・画像検査・fal・動画データは模擬であり、実生成物の品質や実機再生の成功を示さない。診断コードに秘密値や任意の例外本文が混ざらないことも確認した。

## 次の実機確認

実APIへの送信・デプロイは今回実施していない。起動済みの試遊サーバーも停止していない。

1. 現在の試遊を終了し、ターミナルで停止してから `npm.cmd run play:mobile` を実行する。
2. 起動表示でエンディング動画が有効、全プレイヤー合計100回であることを確認する。
3. 新しいプレイを開始して最後まで遊ぶ。以前失敗したプレイの動画は再生成されない。
4. 再び失敗した場合は「不具合報告用の情報」のコードを確認する。これにより次の調査対象を特定する。

## 再試遊後の変更：終了時の完成画像で制作する

同日の再試遊で「動画の元になる最後の場面画像を用意できませんでした」と報告された。旧処理は最終messageIdの画像だけを待ち、失敗・キャンセル時には以前の完成画像があっても停止していた。ユーザー指示に従い、終了時点で完成している画像を素材として固定する方式へ変更した。

- ゲーム内の時点が最新の完成画像を選び、直近行動の行動前画像も同時に固定する。最終画像の未完成・失敗・キャンセルで待ち続けない。文字起こし猶予やジョブの待機中に完成した画像は追加しない。
- 古い画像の時点と、描くべき確定状態を脚本・画像編集・検査へ明示する。最新の行動結果や解除数を画像に合わせて巻き戻さない。
- 完成済みの場面画像が1枚もない場合は素材不足として表示し、falへ送信しない。

課金なしの制作処理テストでは、最終画像完成済み／未完成／失敗／キャンセル、初期画像のみ、終了後の画像完成、完成画像なしを確認した。以前の1行動選択の回帰確認も維持している。画像が完成した順序とゲーム内の順序が逆の場合や、他プレイ・未検査・写真素材を除外することも確認した。

追加修正後の `npm.cmd test` は344件成功、失敗・skipなし。`npm.cmd run build`（型・設定検証を含む）、変更コード7ファイルのPrettier検査、`git diff --check` も成功。AI・fal・動画データは模擬であり、実生成・実機再生の成功は未確認。設定の追加は不要で、上記手順のとおりサーバーを再起動して新しいプレイで確認する。

## 再試遊後の変更：実MP4の音声開始補正に対応する

2026-09-14、再試遊で `ENDING_VIDEO_INVALID_MEDIA` が報告された。falの当該モデルの直近履歴を読み取り、15秒・768Pで生成された実動画を取得した。取得した動画で従来の検査エラーを再現し、AAC音声の時間表とヘッダーの不一致が原因であることを特定した。

| 実動画の情報 | 値 |
| --- | --- |
| サイズ | 8,566,444 bytes |
| 映像 | H.264、768×768 |
| 全体の長さ | 15.104秒 |
| 音声 | AAC、時間単位32,000 ticks/秒 |
| 音声ヘッダーの長さ（mdhd） | 483,328 ticks |
| 音声サンプル時間の合計（stts） | 484,352 ticks |
| 再生開始位置（elst） | 1,024 ticks＝0.032秒 |

差分は音声の再生準備部分を除く指定と一致していた。AACの先頭準備部分と編集リストについては[Appleの説明](https://developer.apple.com/documentation/quicktime-file-format/background_aac_encoding)、編集後の長さをmdhdへ記録する過去のmuxer挙動については[FFmpegの修正記録](https://ffmpeg.org/pipermail/ffmpeg-cvslog/2022-January/130557.html)を確認した。

音声の時間が一致しない場合に限り、単一・等速の編集リストを読み取る。先頭補正は0.5秒以下、差分が開始位置と完全一致し、編集後の長さとトラックの長さも一致する場合だけ許容する。15秒±0.5秒、768角、音声あり、容量・サンプル数・ファイル内参照位置の検査は維持する。

修正後は同じ実動画が検査に合格した。ローカルのHTTP Range配信とChrome headless（390×844）で最後まで再生し、映像・音声のデコード量が両方とも正、再生エラーなしを確認した。消音・4倍速での機械的確認であり、実iPhoneの再生、音の内容、演出の品質評価ではない。

回帰テストは編集リストv0/v1の許容、リスト欠落、開始位置・長さの不一致、負の開始位置、変速、補正上限超過の拒否を確認する。再生成要求は送っておらず、falの生成済み結果を読み取り取得した。動画本体と上流URLはGitへ追加しない。

同時進行中のタグ・物語状態の変更に型・テストの未対応箇所があり、作業フォルダー全体では型検査とテスト8件が失敗した。その変更を編集・取り消さず、`02ba78c`の保存済みコードに今回の検査・テスト3ファイルだけを重ねたコピーで確認したところ、全347件成功・ビルド成功だった。ブランチは切り替えていない。実動画の再生確認も今回の修正の確認であり、同時進行中の変更の検証完了を意味しない。

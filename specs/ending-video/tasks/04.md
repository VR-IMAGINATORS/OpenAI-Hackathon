# 04: 配信設定・回帰検証・運営手順

Issue: https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/38

目的: Web版エンディング動画の配信設定・回帰検証・運営手順。

参照: specs/ending-video/spec.md (E11・E16)、plan.md (P5・P6)、data-model.md、contracts/openapi.json。厳密な型・状態・APIはこれらを参照し、独自に拡張しない。

依存: 設定準備は独立。統合検証は01〜03。

完了条件:
- [x] 対象の承認済み計画と仕様を実装する。
- [x] 関連する課金なしテストを実行する。
- [x] 実API・実機・映像内容の未検証を区別して記録する。

mainへの公開反映、秘密値の閲覧・保存、独立マルチレビューは対象外。

## 進捗

- [x] 実装
- [x] 対象検証

### 設定・配信・手順担当チェックポイント（2026-09-14）

- [x] `EndingConfig`を追加。有効化時のキー/予算必須、回数1〜1,000、締切60〜540秒、並列1〜2、TTLと締切の整合性を検証。
- [x] 無効時TTL300秒、有効時TTL600秒を既定とし、既存環境の動作を維持。
- [x] development/judgingの既存workflowでサーバー配信stepだけにFAL設定を注入。Docker buildへは渡さない。
- [x] deploy scriptのNode strip-only実行を維持。動画無効時はサーバー環境にFALキーを含めない。
- [x] `.env.local.example`、開発/公開手順、初心者向けキー登録手順を更新。judging利用は必須でなく、登録は公開実行の承認ではないことも明記。
- [x] `node --import tsx --test tests/ending-config.test.ts tests/hosted-deploy.test.ts`：16件成功（実API・実配信なし）。
- [x] 指定Prettier 3.6.2をnpm exec経由で使用し、標準format:check成功。
- [x] 全体のcheck/test/build成功、280件成功。模擬APIのHTTP・ブラウザ確認成功。
- [ ] 実fal、実画像編集、実スマホ、5人負荷、映像内容/文字の確認。

秘密ファイル/キー実値は閲覧していない。GitHub/AWSへ設定保存・公開更新は行っていない。

主担当統合確認（2026-09-14）: ローカル実装・課金なし検証完了。詳細と実環境で残る確認は[verification.md](../verification.md)を参照。公開環境へ未反映。

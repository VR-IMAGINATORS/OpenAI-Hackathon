# 03: 認証付き動画配信と再生UI

Issue: https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/37

目的: Web版エンディング動画の認証付き動画配信と再生UI。

参照: specs/ending-video/spec.md (E08〜E10・E12・E14)、plan.md (P4)、data-model.md、contracts/openapi.json。厳密な型・状態・APIはこれらを参照し、独自に拡張しない。

依存: 01/02の公開契約。

完了条件:
- [x] 対象の承認済み計画と仕様を実装する。
- [x] 関連する課金なしテストを実行する。
- [x] 実API・実機・映像内容の未検証を区別して記録する。

mainへの公開反映、秘密値の閲覧・保存、独立マルチレビューは対象外。

## 進捗

- [x] 実装
- [x] 対象検証

### UI担当チェックポイント（2026-09-14）

- [x] `EndingVideo.tsx`と日英表示を追加し、メッセンジャー/既存画面の終了時へ接続。
- [x] `queued` / `preparing` / `generating`の状態取得、終端・認証失効・保持期限での停止、画面離脱時の要求中止。
- [x] `controls` / `playsInline`で手動再生。動画終了または「結果を見る」でサーバー由来の物語だけを表示。
- [x] 確定した結末/解除数/最終行動結果は先に表示し、会話履歴も待機中に閲覧可能。`playId`ごとのキーで再プレイを分離。
- [x] `node node_modules/typescript/bin/tsc --noEmit`（UI接続時点）は成功。
- [x] 模擬APIで手動再生UI・結果表示・再読込・失敗・認証・期限切れをChromeで確認。実動画のデコードは別確認。
- [ ] 実スマホの音声付き再生、実fal動画・画像・物語内容の確認。

HTTP配信/保存/ジョブの統合検証は主担当の記録に従う。上記は実動画生成の成功を示さない。

主担当統合確認（2026-09-14）: ローカル実装・課金なし検証完了。詳細と実環境で残る確認は[verification.md](../verification.md)を参照。公開環境へ未反映。

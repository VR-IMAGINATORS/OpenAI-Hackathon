# 02: 脚本・画像・fal生成ジョブ

Issue: https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/36

目的: Web版エンディング動画の脚本・画像・fal生成ジョブ。

参照: specs/ending-video/spec.md (E03〜E07・E13・E15)、plan.md (P2・P3)、data-model.md、contracts/openapi.json。厳密な型・状態・APIはこれらを参照し、独自に拡張しない。

依存: 01の公開型。

完了条件:
- [x] 対象の承認済み計画と仕様を実装する。
- [x] 関連する課金なしテストを実行する。
- [x] 実API・実機・映像内容の未検証を区別して記録する。

mainへの公開反映、秘密値の閲覧・保存、独立マルチレビューは対象外。

## 進捗

- [x] 実装
- [x] 対象検証

## fal通信・MP4技術検査・制作入力の検証

- [x] `packages/server/fal.ts`: 固定モデル・2画像・15秒768P・安全検査・再送禁止ヘッダー、request IDに限定した照会、停止確認とキャンセル要求の区別、資格を渡さないfal.media受信、ストリーム容量制限と中断。
- [x] `packages/server/ending-video-media.ts`: 最大24MiB、有限MP4 box、15±0.5秒、768角の映像、音声track、ローカルsample参照とmdat範囲を検査。フラグメントMP4は対象外。
- [x] `tests/ending-fal.test.ts` 13件、`tests/ending-pipeline.test.ts` 9件を課金なしで実行し全22件成功。`node node_modules/typescript/bin/tsc --noEmit`成功。
- [x] 序盤を含む提示履歴・全行動入力、未提示情報の除外、不正source ID・行動順の拒否、前画像なしの余韻、画像検査の明示rejectだけ1回修正を確認。小さい正方形画像と96KiB超の末尾記録の境界も確認。
- 実fal・実画像生成・音声内容・スマートフォン再生・映像の意味と文字可読性は未検証。`tests/helpers/ending-mp4.ts`はMP4構造用の合成fixtureであり、実生成成功や再生品質の証拠ではない。

主担当統合確認（2026-09-14）: ローカル実装・課金なし検証完了。詳細と実環境で残る確認は[verification.md](../verification.md)を参照。公開環境へ未反映。

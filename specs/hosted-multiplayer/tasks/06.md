# P6 [hosted-multiplayer] ローカル起動を1サーバーと共通URLのQRへ移行

https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/13

## 目的とスコープ

tools/mobile.ts・tools/dev.ts・package.json・設定例・docs/development.mdを担当。旧relay/adminの不要コード・スクリプトは依存移行を確認して削除する。実際の秘密設定は変更しない。

## 参照元

specs/hosted-multiplayer/ 配下の plan.md §2 P6と旧構成廃止、§6、§10。spec R01/R08。

厳密なAPI・型・状態遷移は正本を参照し、独自に補完しない。

## 依存関係

#11, #12

## 完了条件

- [x] ローカルでアプリ1つとtunnelを起動し、共通公開URLとQRを表示する
- [x] 別relayと別adminポートを起動せず、終了/起動失敗時の子プロセスを整理
- [x] 旧設定から統合設定への手順を記載し、実キーの自動コピーやルート.env読込をしない
- [x] 旧relay/招待専用テストの意図を新構成へ移植し、不要import/起動スクリプト参照を解消
- [x] mockの1サーバー起動とQR/Origin制限を検証


## 実行記録
実装・実mock起動・実HTTPS tunnel/QR検証完了。正常Ctrl+CのWindows実機確認は未実施。

### P6 分担 checkpoint（hosted_web、2026-09-13）
- 親担当: tools/dev.ts・tools/mobile.ts・統合entry・package/tsconfig・README/envと起動検証。
- 本担当: 旧relay/ローカル管理・招待HTTPの依存調査、テスト移植、不要コード削除。
- 移行済み: 旧mobile-integrationの3障害一巡→tests/hosted-full-game.test.ts（実HTTP+写真decode+GameSession、OpenAIだけfake）。旧play-routerのGameAIスキーマ/不正応答とトンネルURL検証→hosted-migration.test.ts。環境ファイル分離/設定優先順位/安全な値→hosted-config.test.ts。
- 旧relayのモデル/要求制限・予算/同時枠・unknown create/close・timeout・上流固定URL検証はhosted-ai.test.ts、Cookie/Origin/操作権/失効/枠はhosted-http.test.tsとhosted-sessions.test.tsへ対応。
- 廃止対象: 一回招待、別admin token/QR、relay token発行/auth-none、relayヘルス診断、local→relay転送に固有のredirect/shape/deadline検証。これらの旧HTTP経路を公開版へ残す意図はなく、旧動作そのもののcoverageは終了。Provider境界の固定URL/上限/redirect拒否は新サービスのテスト対象として維持。
- apps/relayの5ファイル、apps/local-serverの旧app/index/config/admin/play-session/mobile-app/play-router、旧6テストを明示削除。GameSession/clock/photo/liveと既存game/scenarioテストは維持。
- packages/shared/api.tsの旧Relay/Bootstrap型、packages/server/config.tsの未参照relay URL/demo条件分岐、http.tsの未参照旧login/body/server shutdown helperを削除。現役baseApp/errorResponseは維持。
- 検証: 移植7件PASS、全体tsc PASS、2026-09-13時点 npm相当のtsx全61テストPASS。実API/実機/AWS未実施。

### P6 実起動確認 checkpoint（2026-09-13）
- `tools/dev.ts --mock`を秘密設定のない子プロセスで実起動。127.0.0.1:5173の実Appで合言葉不正/正解、HttpOnly/SameSite Cookie、認証だけでは枠なし、モック音声拒否、画像ロード、390px/1280px横溢れなし、明示終了/再プレイをPlaywright確認PASS。
- `tools/mobile.ts --mock`と実cloudflaredでHTTPSトンネルを作成。共通URL（fragment/queryなし）とターミナルQR出力、build済Appの合言葉認証、Secure/HttpOnly/Strict Cookie、モバイル表示を確認PASS。実機でQRを撮影する検証ではない。
- 最初の2回は登録直後のhealth疎通待ちで終了。別診断で初期DNS ENOTFOUND→5秒後health200を観測。親がreadiness上限を45秒へ変更後、実mobile経路がPASS。
- 検証補助scriptはgit対象外artifacts/check-hosted-dev.cjs、check-hosted-tunnel.cjs、check-tunnel-diagnostic.mjs。スクリーンショットhosted-dev-mobile.png、hosted-dev-desktop.png、hosted-tunnel-mobile.pngとモック起動ログを保存。
- 生成したプロセスツリーのみをPID指定で終了し、4310/5173が閉じたことを確認。Windowsの検証helperはtaskkill /Tを使ったため、この成功をCtrl+Cの正常終了検証と混同しない。失敗したmobile経路ではdrain完了ログと自然終了も確認。
- OpenAI API、実マイク/写真端末、AWSは未実施。モック合言葉以外の秘密設定は読まず、起動はHOSTED_NO_ENV_FILE/--mock固定。

# P7 [hosted-multiplayer] LightsailとGitHub Actionsの配信手順を実装

https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/14

## 目的とスコープ

Dockerfile・.dockerignore・tools/deploy.ts・infra/lightsail設定・開発/審査workflow・docs/hosting.mdを担当。設定テンプレートは先行作成可能、実デプロイは統合済みimageと運営初期設定が必要。

## 参照元

specs/hosted-multiplayer/ 配下の plan.md §2 P7、§6〜8、§10、§12。research.md。spec R09〜R12。

厳密なAPI・型・状態遷移は正本を参照し、独自に補完しない。

## 依存関係

#11, #13

## 完了条件

- [x] Linuxの単一プロセスimageを構築し、Micro/scale1/標準HTTPSで起動できる設定を用意
- [x] main自動/審査SHA手動と両Environmentのmain制限・OIDC短期資格を設定手順に含める
- [x] 対象アプリSHAのbuildを秘密なしで行い、配信スクリプトは信頼されたmainを使用
- [x] 旧版drain→終了確認→deploy→image/version確認、失敗時中止と明示復旧を検証
- [ ] 秘密をimage/ログ/artifactに残さず、環境単位の配信排他と最小権限を確認
- [x] 実AWS・OIDC・更新試験と期間終了後の削除手順を検証記録へ分けて記載。未実施を成功扱いしない


## 実行記録
2026-09-13: 設定・スクリプト・手順と自動検証を実装済み。実AWS/OIDCの初期設定待ちのため公開検証は未完了。

### 担当工程チェックリスト
- [x] 承認済み配信計画と公式配布元を確認
- [x] Node digest、Actions SHA、AWS CLI/Lightsailctl版と配布ハッシュを固定
- [x] Docker/秘密除外/IAMテンプレートを作成
- [x] secretless buildとmain trusted deployのworkflowを分離
- [x] drain/配信/確認スクリプトを実装
- [x] fake AWSとHTTPによる配信・中断・秘密非出力の検証
- [x] 統合後Linux Docker build/smoke
- [x] 運営設定・復旧・削除手順を記録
- [ ] 実AWS/OIDC/更新確認（運営設定待ち、未実施）


### 検証記録
- `npx tsx --test tests/hosted-deploy.test.ts`: 7/7成功。これはfake AWS/HTTPであり実AWS成功ではない。
- fake検証: 旧version/boot照合、120秒未確認中止、image/version一致、初回フラグ、失敗後の一時JSON削除、非main資格到達防止のworkflow構造、秘密なしbuildとmain配信分離を確認。
- `docker build --platform linux/amd64 -t call-to-the-past:hosted-smoke .`: 成功。Linux内npm ci/check/Vite/server build、production依存のsharp取得を確認。最初の試行は統合中の旧play-router型不整合で停止し、移行後の再buildで成功した。
- Linux mock HTTP smoke: uid=1000、health/bootstrap/auth=200、sharp JPEG変換270 bytes、image内`.env`/`.env.local`/`.env.relay.local`/artifacts/proposal/specs不在。起動待ちなしの初回fetchはECONNREFUSED、起動待ちを追加した再実行で成功。検証コンテナは停止・削除済み。
- Node公式digestは実pullで確認（v22.23.2）。Actions SHAは各公式repository tag refで確認。AWS CLI2.36.44 zipは公式配布から取得してSHA256記録、Lightsailctl1.0.8は公式release digestを照合用に採用。
- 未実施: AWS/IAM/OIDC初期設定、Environment branch policyの実設定、実CLI最小権限、dev自動/judging手動配信、旧新切替・実Live停止・Micro負荷・AWS/Actionsログ標本、期間終了時の実削除。AWSアカウントは存在するがGitHub連携/Lightsail未設定とのユーザー回答に従い、実リソースは作成していない。
- 配信/廃止の運営手順はdocs/hosting.md。実資格は参照せず、実API/実AWS操作は行っていない。

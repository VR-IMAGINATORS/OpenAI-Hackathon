# P5 [hosted-multiplayer] 公開URL向け参加・復帰・再プレイ画面へ変更

https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/12

## 目的とスコープ

apps/web/src/App.tsx・JoinScreen.tsx・PlayScreen.tsx・live.ts・play-api.tsとpackages/shared/api.tsを担当。契約mockで先行実装可能。導入演出・ゲーム内容の改善は行わない。

## 参照元

specs/hosted-multiplayer/ 配下の plan.md §2 P5、§3〜4、§12。spec §3、R02〜R07。contracts/openapi.yaml。

厳密なAPI・型・状態遷移は正本を参照し、独自に補完しない。

## 依存関係

なし（契約に基づき先行可能）

## 完了条件

- [x] 共通合言葉から開始でき、一回招待fragmentやPC管理操作を必要としない
- [x] マイク許可後に枠を取り、満員/接続失敗/期限切れ/更新失効を区別して案内
- [x] 全プレイ操作へ所有権情報を付け、応答欠落時は同じ要求として再確認する
- [x] 同ブラウザ復帰・別タブへの明示引継ぎ・再プレイを実装し、導入を重複しない
- [x] mockブラウザ検証でモバイル/PC表示、音声再開、写真再送、枠重複防止を確認。実API統合は#15で実施


## 実行記録
実装完了。統合検証はP8で継続。

### P5 実装 checkpoint 2026-09-13
- 着手: 承認済み仕様・計画・データモデル・API契約を確認。
- 手順: 共通envelopeと所有権ヘッダー → 合言葉/開始/復帰 → Live作成冪等性 → 写真同ID再送と失効 → mock検証。
- 課金API・実envは利用しない。


### P5 完了 checkpoint 2026-09-13
- Hosted API envelopeと所有権ヘッダーを共通化。現在入口からFoundation/招待QR依存を除去。
- 合言葉認証後、マイクとSDPを準備してから枠を確保。create/Liveの通信結果不明は同IDで再確認。Live手動再試行でもpeer・SDP・requestIdを保持。
- リロード時は状態のみ取得。「この画面で再接続」の明示操作でtakeover。CONTROL_BUSY/認証失効/期限切れはローカル音声を停止。再プレイは終了確認後のみ。
- 写真応答欠落は同じrequestId、行動応答欠落は同じactionId。既存導入画像/文面/本編の明示開始を維持。
- 2026-09-13 mockブラウザsmoke PASS: 合言葉、マイク拒否（枠なし）、満員（Liveなし）、create/Live応答欠落、Live手動同ID再送、音声再接続、同ブラウザreload枠重複なし、別controller拒否と旧マイク停止、明示引継ぎ、写真同ID再送、行動再確認、モバイル390px/PC1280px、明示終了/再参加入口。
- 初回追加検証は再接続後のbusy解除前に写真を設定してtimeout。ユーザーが押せる状態まで待つテストへ修正して再実行PASS。
- 全体tscではP3移行中の旧play-router/GameAI呼出箇所のみエラー。担当UIの型エラーなし。統合後に親が全体check/buildを実行する。
- 課金API/実スマホ/AWSは未実施。smokeは全API/WebRTCをmock化。pagehide keepaliveにViteのloopback proxy拒否ログあり（実上流へ接続しない）。

# P2 [hosted-multiplayer] 認証・複数プレイ所有権と復帰期限を管理

https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/9

## 目的とスコープ

apps/server/session-store.ts・play-registry.ts・control.tsを新規作成。GameSessionとproviderは差し替え可能な境界で接続し、共有される状態とプレイ専用状態を分ける。

## 参照元

specs/hosted-multiplayer/ 配下の plan.md §2 P2、§3〜6。data-model.md全文。spec R02〜R07。

厳密なAPI・型・状態遷移は正本を参照し、独自に補完しない。

## 依存関係

なし（契約に基づき先行可能）

## 完了条件

- [x] 合言葉認証と枠確保を分離し、閲覧/認証だけではAPIと人数枠を使わない
- [x] 同時5人を原子的に確保し、6人目拒否・終了後の解放・未確認予約維持を検証
- [x] 同一ブラウザの複数タブ/再読み込みで枠や期限を増やさず、他人の状態へアクセスできない
- [x] fake時計で初期接続・切断・最大寿命・再起動失効・結果保持期限を検証


## 実行記録
実装済み。HTTPとの結合はP3/P4で行う。

### 工程チェックリスト（P2担当）
- [x] 承認済み仕様・データモデル・API契約を確認
- [x] 認証ストアと操作権検証を実装
- [x] 原子的な枠確保・復帰期限・終了保留を実装
- [x] fake時計/複数所有者/複数タブのテストを実行

### 検証結果（2026-09-13）
- `npx tsx --test tests/hosted-sessions.test.ts`: 11件成功。fake時計、認証上限、同時5枠、再送、別所有者、操作権移動、期限、終了待ち・隔離を確認。
- 担当4ファイルをPrettierで整形。
- `npx tsc --noEmit`: 担当ファイルのエラーなし。並行移行中の旧play-routerと旧テストでAIResponsesClient境界のエラー4件があり、親へ報告。全体型チェック成功とは扱わない。
- 実API・実環境・実env・commit/pushは未実施。
- runtime本文破棄はdispose hook、provider終了はclose hookへ分離。終了未確認はquarantined枠を保持する。

# P1 [hosted-multiplayer] AI通信と利用制限を内部サービスへ統合

https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/8

## 目的とスコープ

packages/server/openai.ts・ai-service.ts・ai-config.tsを新規作成し、apps/relayのprovider通信と予約制御を移設する。旧relay削除は統合完了タスクが担当。

## 参照元

specs/hosted-multiplayer/ 配下の plan.md §2 P1、§3、§5、§7。spec R01/R13。

厳密なAPI・型・状態遷移は正本を参照し、独自に補完しない。

## 依存関係

なし（契約に基づき先行可能）

## 完了条件

- [x] 既存の要求検証・モデル制限・出力上限を維持し、相互HTTPなしで呼べる
- [x] プレイ別/環境全体の予約を外部await前に行い、失敗で試行回数を戻さない
- [x] 作成結果不明と終了未確認で予約を保持し、終了処理の重複を防ぐ
- [x] fake transportで並行上限・timeout・遅延作成・shutdownを検証


## 実行記録
2026-09-13: P1実装完了。課金なしのfake transportテスト12件成功。

### 担当工程チェックリスト
- [x] 承認済み仕様・計画・データモデル・公開契約、既存provider実装を確認
- [x] 内部AiService APIと設定型を具体化し親へ共有
- [x] transport/schema移設とプレイ別/全体の同期予約・終了処理を実装
- [x] fake transportで制限・作成遅延・timeout・shutdownを検証
- [x] 検証結果を記録し統合担当へ引継ぎ


検証: `npx tsx --test tests/hosted-ai.test.ts` 12/12成功。固定URL/要求検証/出力サイズ、5枠先行予約、回数非返却、timeout後予約保持、遅延作成回収、重複provider ID隔離、3回終了共有、shutdown、期限不変を確認。全体`tsc --noEmit`は統合作業中の旧play-routerとUI型で失敗し、P1所有ファイルの型エラーはなかった。実APIは未実施。

追記: 終了済みプレイの遅延Responsesが完了した時点で予算Mapを掃除する回帰テストを追加。12件成功。

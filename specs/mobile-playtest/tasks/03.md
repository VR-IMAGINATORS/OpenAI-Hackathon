# 03 スマホ音声・写真プレイ画面

## 目的とスコープ
apps/web配下、ブラウザ用検証。plan §5、research.md、contracts/openapi.yamlに沿って招待参加、Live音声、撮影/訂正/明示実行/復帰を実装。モバイル主・PC副。

## 正本
specs/mobile-playtest/spec.md と plan.md、data-model.md、contracts/ を参照。型や契約を独自補完しない。

## 依存
実装は契約ベースで並行可能。04の統合完了は01〜03に依存。

## 完了条件
- [x] 対象実装
- [x] 担当の課金なし検証
- [x] 検証結果と残作業を記録

## 実行サブタスク
- [x] foundation/mobile画面分岐、招待フラグメント即時除去、claimと所有cookieで復帰
- [x] GPT-Live WebRTC接続、公式イベント転送、local append送信、マイク/音声再生エラーと再接続
- [x] カメラ優先/ファイル選択、最大1280px JPEG変換、写真削除/送信再試行
- [x] 公開状態・認識・残資源・在庫・結果、音声変更中の実行無効化、明示実行
- [x] 通信結果不明時の同actionId再送、接続復帰、終端の音声猶予
- [x] fake API/WebRTCのモバイル/PCブラウザ検証

GitHub: https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/issues/4

## 担当検証結果（2026-09-12）
- npx tsc --noEmit: 通過。
- apps/web/tests/mobile-smoke.cjs: bundled Playwright + Chromeで通過。fake API/WebRTCのみ。
- 390×844と1280×900で横溢れなし、主操作44px以上。招待除去、接続、切断後の再接続、reload復帰、開始、Canvas JPEGアップロード、音声文字起こし表示、action通信不明時に同ID再送・行動1回消費、明示終了を確認。
- スクリーンショット: artifacts/mobile-playtest-390.png、artifacts/mobile-playtest-desktop.png（Git対象外）。
- 任意再実行: Viteを127.0.0.1:5178で起動し、PLAYWRIGHT_MODULEに使用可能なPlaywrightモジュールを設定してnode apps/web/tests/mobile-smoke.cjs。ブラウザ依存はこのタスクではpackageに追加しない。
- 実GPT-Live接続・実カメラ/マイク・iOS Safari/Android Chrome・モバイル回線・面白さは未検証。機械検証をこれらの成功とは扱わない。
- 結末音声には最大10秒の接続猶予を設ける。append受付は再生完了ではないため、長い発話がすべて再生される保証は実API検証待ち。
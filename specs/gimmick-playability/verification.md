# ギミック調整の検証記録

2026-09-16。ローカルの変更を対象とする。実AIの成功や画像品質を模擬結果で代用しない。

## 変更箇所と原因

| 項目         | 原因・正本                                                                                       | 変更                                                                                                                                                     |
| ------------ | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 工夫の成否   | `creative-acceptance.ts` のAI分類後にサーバーが1/3抽選していた                                   | 抽選処理を削除。旧 `successProbability` は互換読込後に捨てる。目的につながる無茶案は検証後に成功を確定する。猫の写真の爪を使う例を認識・判断promptへ追加 |
| 説明とヒント | `situationDisplay` と `hints`、Liveと調査promptに要求時のみヒントという指定                      | 初級ヒントを説明直後へ表示する共通処理を追加。既定3ギミックの日英文を平易にし、音声の説明順序も統一                                                      |
| 状況画像     | `image-service.ts` が直前行動の描写を優先。途中の `public-scene.ts` は現在障害の説明が欠けていた | 現障害の前提を満たす公開説明と状態を渡し、進行中は前行動を入力から除く。生成と検査の対象を一致させ、終幕の確定出口は維持                                 |

`GameSession` による現在障害の許可遷移・在庫ID・完了条件の検証は維持する。実行の明示中止、一度だけの確定、認証・利用上限も撤去しない。

## 自動確認（初回調整）

初回調整の統合確認は2026-09-16に実施した。

| 確認                                       | 結果                                                        |
| ------------------------------------------ | ----------------------------------------------------------- |
| `npm test`                                 | 938件成功、失敗・中断・スキップ0件                          |
| `npm run check`（build内で実行）           | 型・標準シナリオ・18構成のstory catalog・共通設定の検証成功 |
| `npm run build`                            | UIとサーバーのビルド成功                                    |
| 変更ファイルのPrettier、`git diff --check` | 成功                                                        |
| Solによる独立レビュー                      | 指摘を修正・再確認し、未解決の具体的な指摘なし              |

- 全体テストに、抽選廃止の単体・既定シナリオの猫写真例14件、旧成功率0/1の互換設定、写真と音声の実行、再試行・中止・不正状態変更を含む。モデル応答は模擬であり、写真内容を実AIが識別した証拠ではない。
- 初回・次の障害・部分進展・終了の説明とヒント、繰り返し失敗した場合のヒント重複防止、全18構成の日英Live入力上限を確認した。
- 2段目解除／3段目未解除、部分進展、最終クリアの生成・検査入力を確認した。過去障害だけを条件とする `publicVisuals` は進行中・終端の両方で除外し、全体共通のvisualと確定した最終出口は維持した。別途生成するending画像の既知の意味も維持した。
- 独立レビューでは、猫そのものが行動する表現、Live入力の上限超過、過去障害visualの混入を修正した。
- 統合途中のテスト停止は、変更後の説明文と旧テストの期待値の不一致が後処理待機で隠れていたことが原因だった。説明とヒントの構造を確認する期待値へ更新した。今回のガイド追加によるAI呼出回数の増加はない。

## 初回調整時のヒント文（変更履歴）

説明は小学校高学年を基準とする。写真を撮るゲームに合わせ、道具は「考えてみよう」から「探してみよう」へ揃えた。閂の基本ヒントは次の文面に更新した。

> ネジの直線的な溝に合う、先が平たいものを探してみよう。ネジを外して金具をどければ、扉を閉じている横向きの棒を動かせそうだよ。

## 本番確認後の口調調整（2026-09-16）

ユーザーの本番確認後の希望に合わせ、「ヒント:」表示と「探してみよう」という直接指示を外す。説明の後に、メイの「欲しいもの＋それで起こせそうな変化」という独り言を添える。現在のWeb既定の3ギミックでは初級から追加の段階ヒントまで日英を統一し、公開段階・対象・作用は維持する。窓の初級案内は次の文面とする。

> 窓のすき間から奥の輪まで届くものがあればなぁ…。輪を手前に引くと、扉を閉じている棒も動くと思うんだ。

固定表示・追加相談の文章結合と、Live・調査のpromptを更新する。実機音声の口調と配信後の表示は、今回のローカル検証とは別に確認する。

- 関連7ファイルのテスト113件成功。初回の日英HTTP表示、追加相談、部分進展、次の障害、終了時の手がかり除去、重複防止、Live入力上限を確認した。音声・AI応答は模擬。
- `npm run build` 成功（型・標準シナリオ・18構成のカタログ検証を含む）。英語Live指示が上限を超えた初回テストを受け、指示を同じ意味で短くして接続テストを再確認した。
- シナリオの差分はヒント54文字列のみ。障害側9件と通常/partialの知識18件が日英とも一致し、ほかの条件・データが変わっていないことを確認した。

## ローカルとAWS

- `apps/server/scenario-catalog.ts` の既定は `scenarios/playtest/warehouse-expanded-r1.json`。`SCENARIO_PATH` 明示指定時のみ別シナリオを使う。共通設定は `config/game-core.json`。
- シナリオ・共通設定は新規プレイ時に読込・固定される。進行中プレイやすでに受付済みの画像ジョブには途中から差し替えない。
- `Dockerfile` が `scenarios/` と `config/` を配信用imageへ同梱する。通常の `tools/deploy.ts`／workflowは `SCENARIO_PATH` を別途設定せず、AWS専用のギミックデータを参照しない。
- GitHub APIで確認した最新の[Deploy development成功記録](https://github.com/VR-IMAGINATORS/OpenAI-Hackathon/actions/runs/34988833461)は `d3c9d9dd783c31a25a28160f01c8d702f4c988bb`、2026-09-16 00:36:08 JST。同SHAのソースも拡充シナリオを既定にしている。
- AWSの現在稼働中コンテナ・手動変更・全環境の状態は直接照会していない。配信履歴の成功は、その後の手動変更がない証明ではない。今回の差分は未配信。

## 実機確認で残るもの

実APIの認識・判定、GPT-Liveの声、画像生成結果の意味的な正確さ、スマホでの操作は未実施。配信後は新しいプレイで次を確認する。

1. 猫写真を送って「爪で目隠しを切って」と伝える。写真だけの自動実行と写真＋音声のどちらも、通常発話で中止を繰り返さない。
2. 各ギミックで短い説明の直後に一つの具体的なヒントが表示される。部分進展後に元の状態へ説明が戻らず、終了後には次のヒントが出ない。
3. 2段目解除後は3段目の閉じた出口と固定金具を描き、前の開いた内扉を主役にしない。最終クリア後は開いた出口を描く。

1/3へ戻す手順は廃止。実AIの試行が少ない段階で、全写真・全表現の成功や画像の完全な正確さは主張しない。

## 現実性と結果説明の再調整（2026-09-16）

基点 `0a270ca`、作業ブランチ `codex/grounded-item-acceptance`。今回の変更はローカルのみで、コミット・配信は未実施。

- 共通設定と認識後の受付・最終判定の指示を同期。現物で成立する具体手順を必須とし、長さ・強度・大きさ・摩擦・精度の不足、未提供部品や勝手な改造での救済を禁止した。「理論上ゼロではない」だけでは通さない。
- `ordinary` の通常判定と、`stretch` に分類された案の完全成功・抽選なしを維持。猫爪も対象の材質と使い方から判断し、一律成功の例を廃止した。今回の成功・失敗の例は模擬応答による処理検証であり、実AIの判断品質は未検証。
- providerの全判定に `actionExplanation` を必須化。性質と作用の有限の組合せ、理由、サーバー検証後の成否・公開状態から説明を作る。成功・部分進展と「作用なし」の矛盾を拒否し、秘密の自由文や未開示のギミック内部名をLiveへ渡さない。
- 結果通知に確定行動のID・公開対象名・用途・道具を添え、次の未解除障害の状況と区別する。旧アダプターの説明欠落や矛盾する成功候補は、理由を捏造せず未特定と説明する。stretch専用の誇張ナレーションは共通説明へ統合した。通常経路のAI呼出回数は増やしていない。
- `npm test`: **964件成功、失敗・中断・スキップ0件**。成否・部分進展・既存道具・明示停止・再試行・公開境界・Live入力上限・終了処理を含む。
- `npm run build`: 成功。型検査、標準シナリオ・18構成・共通設定の検証、UIとサーバーのビルドを含む。
- Solの独立レビューで内部名の公開、旧アダプター自由文の通過、部分進展と失敗理由の矛盾を修正し、限定再確認で未解決指摘なし。
- 初回の英語Live文字数超過は意味を維持した短縮で修正。統合テストで見つかった旧モックの必須項目欠落と自由文の期待値は、新しい公開契約へ同期した。

次の試遊では、溝に合う硬貨、届かない短棒、明示した接続・延長、猫爪と布の相性を比較する。成功・失敗とも性質→作用→結果が伝わり、次の仕掛けの解除を先取りしないことを確認する。実API・GPT-Liveの音声・スマホ・AWSでの確認は今回未実施。
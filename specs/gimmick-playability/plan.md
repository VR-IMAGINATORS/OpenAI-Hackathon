Status: completed locally (2026-09-16)

# 実装計画

ユーザーの3点の調整依頼と、抽選を撤廃する明示回答に基づく。開始時は `fix-block-function`、HEAD `d3869c6`、作業ツリーは空。ローカル実装と模擬検証を完了条件とし、実AI利用・AWS配信・Gitへのコミットは今回の作業に含めない。リポジトリのAGENTS.mdに従い、Astraが方針を統合、Solが画像と説明を実装し、別のSolが独立レビューする。調査と文書整理はTerraへ分担した。

## 実装方針

1. `packages/shared/core-config.ts` と `config/game-core.json` で、創造的工夫を有効/無効だけで扱い、旧 `successProbability` を互換読込後に破棄する。AWS deployment環境から抽選値を渡さない。
2. `apps/local-server/creative-acceptance.ts`、`game.ts`、`game-ai.ts`、入口の指示を更新し、目的との因果関係がある再構成道具を許容し、乱数・固定失敗を撤廃する。G3の除外条件と明示停止は維持する。
3. シナリオ/説明/ヒント/画像入力を更新し、小学校高学年向けの分かりやすい説明の直後に現在障害の初級ヒントを必ず添える。画像は現未解除対象の形状と状態を優先し、前障害の解除描写を入力から除く。最終クリアの画像は維持する。
4. 単体・HTTP・シナリオ検証を追加または更新し、`npm run check`、`npm test`、`npm run build` を実施する。実AI、実音声、画像品質、スマホ、AWSは別の手動検証として記録する。

## 依存とリスク

判定、説明、画像入力は担当ファイルを分けて並列に進め、統合後に確認する。実AIが猫写真の形と働き、子供向け説明、ヒントを正しく解釈するかは自動テストでは証明できない。AWS反映はmain更新後の通常workflowによるため、本番の現在状態は配信後に別途確認する。

## 現実的な受け入れ判定への再調整

基点 `0a270ca`、作業ツリー空から `codex/grounded-item-acceptance` で開始。ユーザーが基準・例・説明方針を採用済みのため、その範囲を実装する。`stretch` の完全成功は明示的に維持する。

1. 共通設定と写真・音声・判定の指示から、物理的な不足の救済・現実性不要・猫爪の一律成功を除く。Solが許容方針、主担当が結果説明と統合を担当する。
2. 結果説明を性質・作用・結果で構成する。秘密を含む判定自由文を公開する方法や、新たなAI呼び出しは使わない。
3. 状態検証・停止・再試行・段階ヒント・画像の対象は維持し、日英・成功・部分進展・失敗・次障害への移行を検証する。別のSolが独立レビューする。
4. 型・設定・関連テスト・ビルドを確認する。実AIの判定の厳しさと音声説明は別途テストプレイで評価する。コミット・配信はこの変更の完了条件に含めない。

# 検証記録

2026-09-12。自動テスト、独立した手順確認、実写真/実画像/実動画の通し、ユーザーの主観評価を区別する。

## 設計

独立Solが正式仕様をレビュー。判定の在庫変更範囲、構造化した可解性基準、メディア省略時の状態、保存先境界の4点を指摘。仕様と実装依頼へ反映済み。実装での確認は後続。

## 実装と形式検証

初期実装では下記を実行し38件成功（ゲーム16、マスター6、メディア16）。この後に独立レビューと回帰追加を行い、最終結果は後段に記録する。

```powershell
python -X utf8 -m unittest discover -s skills/call-to-past/tests -v
python -X utf8 C:/Users/kimha/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/call-to-past
```

スキル形式は`Skill is valid!`。ゲームテストは全エンド境界、0枚、2物体と失敗保持、複合元の再使用拒否、破損後も使える形状、消費済み拒否、冪等性、未公開障害分離、画像版管理とwaiver、履歴からのタイトル保存、リトライを含む。メディアテストは素材/承認改変、価格鮮度と数値型、1度だけの送信、アップロード失敗、ID欠落、同じIDのダウンロード再開、秘密値の伏字をスタブで確認。

ふだんの`unittest discover`がマスターの関数形式テストを拾わなかったため、標準unittestから6件を検出するload_testsを追加した。pytestの追加導入は不要。

指定資格情報ファイルのfalセクションは候補1件として読み出し可能と確認。キー値は出力せず、配布対象のメディア実装・テスト・説明内の実キー一致は0件。これは認証やfal残高の検証ではない。

## 実生成・ユーザー評価

未実施。ユーザーのプレイ用写真はまだ受領していない。各ターンのFlare画像生成と最後の15秒H3動画の具体的な承認・送信・視聴、楽しさ評価は後続。テスト用写真や模擬メディアでこの項目を合格にしない。

以下は旧native経路を採用していた時点の履歴であり、現行のFlare経路の検証ではない。native生成経路だけを別途実測した。検証用の倉庫・はさみ・切れた縄の画像1枚を生成し、tool output_hintに保存先が返り、ローカルコピーとSHA-256確認ができた。`runs/native-tool-check/receipt.json`にプロンプトと目視範囲を保存。顔は見えず、はさみと切れ端は描かれたが、椅子に縄が残り完全な解放は曖昧なので成功ターン画像としては採用していない。実写真入力経路は未検証。パスなし添付の環境では同じ原本のローカル保存を依頼する境界をスキルに明記。

SpecWorkflowは取得したスキル本文を適用した。プラグインのインストールとフック、review-orchestrator一式の動作を確認済みとはしていない。

## 独立リハーサル

実装を担当していないSol highが、スキルと直接参照文書だけから開始・4行動・物語保存・文章版result・同設定retryまで実行した。CLIエラー・状態破損は観測しなかった。失敗時の道具保持、0写真での持越品再使用、損傷状態の継承、同設定retryのseed/master/scenario一致と進行初期化を確認。`runs/independent-forward-test/REPORT.md`と各セッションへ証跡を保持する。

これは合成画像内のはさみを指定した模擬入力で、実写真ではない。明示的combine、通常プレイの別Sol判定呼出し、実メディアはこのリハーサルに含まない。メディア免除はこのテストだけの許可で、通常プレイの既定を変えない。

## 配布前レビュー

別Solが6件を独立再現した。最終画像ではなく初回画像を選ぶ問題、revisionのbool受理、resolve前のreparse検査漏れ、物語packetの最終在庫不足、進行後のイベント画像差替、H3回収証跡とゲーム動画の未結合を修正した。最後のイベント画像が免除なら古い画像へ戻さず未設定を返す。追加の独立確認で見つかった送信後の承認文改変とreceipt動画のリンク境界も修正。再現は`runs/review/final-1789148029`。

rootが最終修正後に全体テストを実行し、**48件成功・skip 0**（ゲーム21、マスター6、メディア21、6.890秒）。形式検証は`Skill is valid!`、git diff --checkも成功。最後の独立レビューで追加された成功送信記録の照合、イベントメディア未確定時の終了処理拒否、waive画像の開始フレーム除外も修正し、rootが差分と回帰テストを確認した。

live動画は承認マニフェスト・送信時承認SHA・request ID・成功送信記録・開始/終了画像・取得動画の照合と、ffprobeによる15秒±0.25秒/768寸法/音声stream存在を通す。ファイルの出所照合はローカル監査であり、手動で全ローカル証跡を書き換える利用者への暗号署名保証ではない。テスト内のlive進行では動画検査をスタブにしており、これで実生成・映像品質の合格とはしない。

## 模擬履歴を使うH3実生成の準備

ユーザーの「模擬テストの結果で、H3での動画生成は可能？」を受け、同じ模擬履歴に沿う開始・終了フレームをnative画像生成で各1枚用意し目視した。各1672×941、同じ人物・衣装・地下室・2本のはさみ・手形を維持。`runs/h3-rehearsal-pilot/`に保存。これは実写真の通し試験を代替しない。

2026-09-12 02:35 JSTに[fal公式](https://fal.ai/models/minimax/h3-max-turbo/image-to-video)の768P単価0.01 USD/秒を確認、15秒1本0.15 USD、[9月11日為替](https://ca.investing.com/currencies/usd-jpy-historical-data)1 USD=154.39円で23.16円。価格は9月14日にプロモーション終了予定。承認用の素材・プロンプト・見積を固定したが、承認記録・アップロード・生成送信・認証・残高確認は未実施。

その後ユーザーが全エンドで開始と終了に大きな差を求めたため、仕様AC10とending.mdへ標準要件として反映。新しい脱出エンドの映像テスト案を`runs/h3-motion-pilot-v2/`に準備した。開始は暗い上り階段、終了は扉を押し開け明るい屋外へ出る場面で、人物の位置・姿勢・構図・景色・照明が大きく変わる。元リハーサルの確定結果は変更していない。旧案は未送信のまま保持し、送信候補はv2だけ。各フレームは目視済みだが、15秒の移動・はさみの持替え・カメラ連続性は実動画で確認する。

## 配布・インストール

`tools/package_skill.py`を実行し、`dist/call-to-past-0.1.0.zip`と`dist/call-to-past-0.1.0.manifest.json`を作成。14ファイルの作業ツリー・ZIP内・インストール先SHA-256が一致した。インストール先は`C:/Users/kimha/.codex/skills/call-to-past/`。インストール済みSKILL形式検証も成功。新しいCodexセッションからの自動検出はまだ実測していない。

ZIP SHA-256: `5e31196eed3e49e79f174849d2a021839b8cf02246354654268ab341856ae489`。

配布ZIP全14ファイルに実falキーの一致は0件。`runs/`と`dist/`はgit check-ignoreで対象と確認。原本・旧案・模擬履歴を保持し、Gitコミット・push・外部共有は行っていない。T08の残りはユーザー写真による実プレイ、承認済みH3生成・動画QA、ユーザー評価。

## H3実生成完了・ユーザー確認

2026-09-12、ユーザーの「そのまま動画生成まで頼む」によりv2を1回送信し、同じrequest IDから回収した。IDは`01a0919c-b34f-76d0-938c-302749255651`。生成モデルはminimax/h3-max-turbo/image-to-video、15秒・768P・balanced。実ファイルは15.104秒、1344×768、24fps、AAC 32kHz stereo。尺/A-V差/音声streamの技術検査、承認と回収receipt照合、game側の実receipt/ffprobe検証を通過した。追加リクエストなし。

納品動画: `runs/h3-motion-pilot-v2/Call-to-the-Past_motion-test_v2.mp4`。SHA-256: `88ba802351250ce8742ff4a450ed60c8c5bfe4da9ede711657150611b07a2237`。動画料金見積は0.15 USD/23.16円。請求明細による実請求額は未確認。

rootと別SolがCRVの29採用フレームと終盤3画像を確認し、階段上り・追従カメラ・扉開放・明るい屋外への大きな変化、人物/衣装の連続性を確認した。標本内で顔・目の露出や目立つ手足の破綻は見られない。はさみの刃に軽い簡略化がある。全フレーム視聴ではなく、エージェントの音声入力は未対応のため試聴は未実施。

ユーザーが動画を視聴し「動画は私の方で確認したので、一旦もうOKとする。」と確認。今回の映像テストは一旦受入済みとする。元の模擬プレイの点数・履歴は変えず、実ユーザー写真を使うゲーム全行程の確認とは区別する。証跡は`ending-h3-001/qc/completion-record.json`。
## H3 Max Turboランタイム同梱更新（0.2.1）

2026-09-12、チームメンバー環境で終了時に`h3-video`が見つからず動画生成できなかった事象へ対応。`estimate_cost.py`、`generate_h3.py`、`verify_and_concat.py`を`call-to-past/scripts/`へ同梱し、`media.py`は自身のスキルルートだけを解決するよう変更した。本番CLIの`--h3-skill`は削除し、FAL_KEYを任意の外部スクリプトへ渡せない境界にした。見積・送信・QCの3本のいずれかが欠ける場合は、欠落名を示して再インストールを案内する。

実装履歴を持たないSol highの独立レビューで、外部ランタイム上書きの秘密情報境界、3ファイル一括検査、孤立コピー回帰、配布先ハッシュ検証を指摘され、すべて反映した。外部`h3-video`のない一時コピーでprepare→approve→submit→status→resultを模擬実行し、全3操作がコピー内の`generate_h3.py`を呼ぶことを確認した。

最終結果は自動テスト51件成功・skip 0、Python構文検査成功、`Skill is valid!`、`git diff --check`成功。正本・`.skill-staging`・インストール先の配布対象17ファイルはmanifestのSHA-256と一致。ZIPに3本を含み、`__pycache__`/`.pyc`は含まない。

配布物: `dist/call-to-past-0.2.1.zip`。SHA-256: `6b5990b7360caae295a023e24dbaa72eb6f3811b30c6cfecb7855a414d8d95e4`。

この更新ではfalへの送信や課金、実動画生成は行っていない。別途必要なのは`fal_client`、`FAL_KEY`、ffmpeg/ffprobeであり、価格再確認と具体的な送信承認も従来どおり必要。

## Flare画像生成preflight更新

2026-09-12、画像生成前にOpenAI SDKを準備・診断する提案を反映。scripts/flare_image.pyを追加し、uvの隔離環境で openai>=2.26,<3 と pillow>=11,<13 を用意する。preflight-onlyはAPIを呼ばず、依存版、OpenAI用キー、UTF-8プロンプト、参照画像、リンクを含まない新規出力先、SDK引数を確認する。

独立Solレビューで、任意のsk-*取得、承認hashと送信バイトの差し替え余地、出力競合、自動再送、価格・時刻型の不足を指摘。修正後は、プロンプトと画像を一度だけ不変バイト列へ読み込み、同じバイトでhashとAPI入力を構成する。完全一致のOpenAI資格情報だけを採用し、承認JSON、24時間以内の価格確認、O_EXCL出力予約、max_retries=0、厳密Base64/PNG検証、not_sent/maybe_sent/response_receivedを実装した。

実プレイの既存プロンプトと画像2枚でpreflightを実行し ready=true、send_state=not_sent を確認。画像APIへの送信と新規出力は0件。全62件（既存51件、新規preflight 11件）成功、skip 0。quick_validateは Skill is valid!。これはローカル事前診断の検証であり、新たな画像生成・課金・モデル利用可否確認ではない。
## Flare preflight 0.3.0 配布・インストール

独立Solの最終判定はApprove（Blocker/High/Mediumなし）。正本で全62件成功・skip 0、quick_validate成功。dist/call-to-past-0.3.0.zipは19ファイル、archive SHA-256 48208b26157b1ff5a5ecc4a1a32ce04ea818552f998db091fef2743d49de2c13。インストール先の全19ファイルをmanifestと照合し不一致0件。

更新後スキル自身で実生成テスト用generate preflightを実行。gpt-image-2.5-flare、low、1536x1024、n=1、max_retries=0としてready=true、send_state=not_sentを確認後、利用者へ正確な内容と概算0.02 USDを提示し「OK」の承認を取得。承認JSONとrequest manifestを照合して1回だけ送信し、response_received、request ID req_801687a7a53a4fa683ab31b97da20d7d、13.984秒、PNG 1536x1024、2,278,897 bytesを確認した。SHA-256はC84517345B57DA857DECB1A6F75E83D6275D21588DE9489533FECFCBFF62F200。目視で空の廃病院隔離室、診察椅子、曇った観察窓、閉じた非常扉、緑のランプを確認し、人物・手持ち道具・可読文字・UI・流血・追加パズルは見られなかった。再送0件。実請求額は請求明細未確認。
## スキル配置変更（2026-09-12）

`.agents/skills/call-to-past/` へ移動。スクリプト・同梱素材は読み込んだ SKILL.md の親フォルダ、プレイ記録は作業フォルダを基準に統一した。上記の旧配置パスは当時の検証記録として残す。

`py -X utf8 -m unittest discover -s .agents/skills/call-to-past/tests -v` を実行し、62件すべて成功。README とスキル内 Markdown のローカルリンクも存在確認済み。有料API送信、実画像・実動画生成、新規セッションでの自動検出は今回未実施。

## 2026-09-13 エンディングのコア方針

会話版の物語手順とAC10を修正。カット数を固定せず、最後のシーンで成否と未脱出の理由を明かし、それまで緊張を保つ。生成前にこの3観点の設計レビューを必須化。終端packetにescape_evidenceを追加し、未解除機構と試行有無を制作に渡す。

unittest 63件成功（課金なし）、git diff --check成功。最後のセッションから別版の設計・自己レビュー・開始/終了画像・動画プロンプト・承認manifestを作成。画像は内蔵imagegenで開始1枚・終了1枚・終了修正1枚。修正後の選択画像は両方1672×941。H3のprepare成功、submit/approveは未実施。動画生成・全編視聴・視聴者理解・ユーザー採用は未検証。既存stateと登録済みメディアは変更していない。

## 2026-09-13 空間設計からのプロンプト出力

staging.pyを追加。共通の座標・寸法・支持・直線軌道・固定カメラとカットのJSONから、平面/側面の時間プレビュー、カメラ図、動画プロンプトを出力する。出力先は新規ディレクトリのみ。連続区間のAABB衝突、室内境界、速度、カメラ衝突、前半の結末装置の画角混入を検査。力学/人体/把持/反射/生成モデルの挙動は保証しない。

全70件のunittest成功、git diff --check成功。Playwrightでプレビューの時刻操作、物体移動、カット変更、再生進行とJSエラーなしを確認。最初に再生時刻がスライダーの丸めで進まない不具合を発見し、連続時間を別保持して修正・再検証した。

最後のセッションでstaging-v3.json→compiled-v4を実行。カメラ図の自己レビューで前景の遮蔽と最後の人物画角外を修正した。内蔵imagegenで図と外見参照から開始/終了各1枚を実生成、両方1672×941。図からの厳密レンダリングではなく配置に生成誤差あり。H3のprepare成功、外部動画は未送信・未生成。生成後の内容確認は人が担当する指示をスキル・手順・AC10に同期。セリフ/ナレーション/歌/BGMを禁止。

空間設計版の実動画回収（2026-09-13 JST）: ユーザー承認後、H3 Turbo I2Vへ15秒・768P・balancedを1回送信、request ID 01a0965e-a1db-7ba3-ad94-4f61280a3060で回収成功。空間設計→プロンプト→参照画像→実API→ファイル回収を通した。ユーザー指示により完成動画の映像/音声レビューは未実施。物理的な動きの改善、3観点の達成、ユーザー採用は未確認。

## 2026-09-13 映画制作・H3文字生成のブランチ保存

人物の目的から場面を設計し、最後のエンドタイトルとVFXをH3で本編と同時生成する方針を追加。終了画像省略をmedia.pyの準備・送信・receipt照合に対応。通常プレイ登録・Web統合の完了とは扱わない。

映画版と文字入り版を実生成。文字入り版はrequest ID `01a09a93-0f20-7890-9353-d27174b6fde2`、15秒・768P・balanced・開始画像のみ。回収とreceipt/動画ハッシュ照合済み。内容品質・採用は人のレビュー待ち。動画・写真・承認記録はGit対象外のrunsに保持する。

ブランチ保存前に `py -X utf8 -m unittest discover -s .agents/skills/call-to-past/tests` を実行し72件成功。課金なしの自動テストであり、新しいゲームの実プレイ検証ではない。

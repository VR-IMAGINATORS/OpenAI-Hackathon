# P5/P6 — 結果の寿命・配置設定・試遊手順

Status: implemented; automated verification passed; actual deployment/device verification pending (2026-09-13)

- [x] 本編start/各action/時計による敗北を固定messageIdへ記録し画像job登録。最後のactionによる勝敗は重複登録しない。
- [x] 元transcriptを表示台帳へ接続し、写真は正規化サムネイルで保存。同一photo requestの再送は重複表示しない。
- [x] ゲーム終了から最大5分の結果保持。read-only結果認証とセッション再読は認証期限を延長しない。新規書込みは通常認証が必要。
- [x] 手動終了/接続失効/絶対期限で未開始画像を中止。既に始まった画像は元deadline内で受信。正常勝敗は既存画像を継続。
- [x] evictionは画像permit取消、drainは待機破棄とabort。media実busyをreadyToDeployへ加算、resumeも連携。
- [x] locale再送一致、stateVersion、開発限定trace、owner単位80 burst/40events秒の受付上限を統合。
- [x] 環境例/両workflow/deploy.tsへ画像と検査の独立予算・モデル・期限・保持時間を追加。本番予算未設定なら配置準備で拒否。
- [x] READMEと開発/構成/配置文書からdocs/game-core.mdへ現行手順を案内。
- [x] 172テスト、build、変更コードformat確認。Chrome日英390px/1280pxと実HTTP統合（AI/メディアfake）を確認。
- [ ] 実アカウントの画像生成/検査モデル利用確認と品質評価、実スマホ、AWS同時5人、実デプロイdrain。

認証と画像の統合証拠はtests/core-result-http.test.ts、core-scene-http.test.ts、core-full-game.test.ts。実HTTP+Chromeの追加確認はローカルartifacts/core-integrated-browser.tsとcore-integrated-mobile.png（有料境界はfake）。画像の意味的精度・会話の面白さを自動成功扱いしない。

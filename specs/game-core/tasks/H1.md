# H1: 委譲監視と期限切れ通知

対象: `apps/local-server/intent-coordinator.ts`、`tests/core-intent.test.ts`。

- [x] 1 秒 heartbeat から `tick()` を呼べるようにし、後続イベントがない場合も実委譲の期限切れを一度通知。
- [x] 未消費のユーザー発話が 3 秒間変わらず、実委譲がない場合に同一 worker で読み取り専用分類。
- [x] 読み取り専用分類は `delegation: null`。実行・根拠消費は行わず、実委譲のみ実行可能。
- [x] 監視分類は同一 context 一回、全体で 10 秒に一回まで。
- [x] execute 相当の監視結果で実委譲を促した後、20 秒未解決なら一度通知。相談・言いかけではこの通知を行わない。
- [x] 訂正、ゲーム状態、世代、操作権変更、reset/stop、遅延結果を検証して古い結果を破棄。
- [x] 新しい実委譲は監視分類と並列起動せず、同一 worker で順に処理。
- [x] `node --import tsx --test tests/core-intent.test.ts`: 18 件成功（実 AI・音声検証ではない）。

公開 callback: `onExpired(delegation)`、`onMissingDelegation(decision, context)`、`onRecoveryExpired(context)`。通知内容とプレイ中・接続中の heartbeat 選別は runtime が担当。
- [x] 実委譲期限切れ後は、新しいユーザー発話根拠が来るまで監視による再委譲要求を停止。assistant 発話・設定更新だけでは再開しない。同じ根拠に対する複数委譲の期限切れ通知は一度に集約。
- 追加回帰検証: `node --import tsx --test tests/core-intent.test.ts` 19 件成功。
- [x] 期限切れ時だけ対象根拠を ledger.consume で無効化し、遅れて新しい実委譲 ID が到着しても古い指示を実行できないようにした。読み取り専用分類そのものは消費しない。
- [x] 実委譲ごとに直近の分類 context を保持し、期限切れ時に後から届いた訂正発話を巻き込まない。復旧待ち期限切れも通知前に対象根拠を無効化する。
- 追加回帰検証: `node --import tsx --test tests/core-intent.test.ts` 21 件成功。
- [x] 古い非同期失敗は epoch・active 状態で除外。実分類は評価中/期限内/同一 context、監視分類は同一 context を追加検証してからエラー通知する。
- 追加回帰検証: reset 後に分類・監視・実行の Promise が reject しても新セッションへ通知しないことを確認。`node --import tsx --test tests/core-intent.test.ts` 22 件成功。

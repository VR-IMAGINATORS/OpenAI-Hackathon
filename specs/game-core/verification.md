# Game core 検証記録

2026-09-13。P0検証ハーネスを実装。日本語の相談→指示・言い直し・途中の間の計3セッションを実Liveで確認済み。全24会話のゲートは未完了。本番のゲームループを自動実行へ切り替えた状態ではない。

## 再開・実マイク確認

1. PCのリポジトリで `npm run probe:live` を実行する。サーバーは `.env.local` の運営設定を使用する。私有のルート `.env` は読まない。
2. ターミナルに表示されるフラグメント付きURLをPCのChromeで開く。127.0.0.1専用。トークンは同じタブのsessionStorageに保持するため再読込できる。サーバー再起動後や新しいタブで認証エラーになったら、ターミナルの最新URLから開き直す。
3. 検証会話を選択。ハサミを前提とする例ではハサミの写真を1枚選ぶ。「マイクで検証開始」を押し、マイクを許可する。
4. AIの「これをどう使う？」に続けて例文を声で話す。最初の簡易試験は「このハサミでロープを切れるかな？」→「じゃあ、そのハサミで切って」。相談でactionが増えず、後の指示で一度だけ増えるかを確認する。
5. 「結果JSONを保存」で記録を保存し、「接続を終了」。次の会話例へ進む。JSONは会話原文を含むためローカルの `artifacts/` で扱い、Gitに追加しない。
6. 24会話を確認し、誤消費・二重実行・未完文の実行・誤った訂正処理があれば記録する。発話の聞こえ方と待ち時間の体感も別途記録する。
7. サーバーはCtrl+Cで停止する。

画面だけを見る場合は `npm run probe:preview`。API設定を読み込まず、開始ボタンを無効化する。PowerShellのnpm引数転送に依存しないよう、実APIフラグは `probe:live` スクリプト自身に含める。

## 試験の範囲と上限

- 1接続あたり写真1枚・障害1個・最大120秒。起動後最大24 Live接続、96 Responses、1接続最大8 Responses。既存envの上限が小さい場合は小さい方を採用。
- 開始ボタンを押すまでは実API通信を開始しない。終了時はprobeを失効させ、上流Liveの終了を要求する。終了を確認できない場合は次の接続を許可しない。
- 入力イベントのHTTP受付は202。分類・行動判断を待たない。判定は既存AiServiceとResponsesの制限内で実行する。
- 原文断片、委譲、分類、予約、確定の時刻をJSONに保存する。ブラウザのrawEvents.receivedAtとハーネスのmeasurements.atを使い、委譲待ち・分類・行動判定を分けて集計する。プロバイダーのstart_ms/end_msは別の時計なので直接差し引かない。
- この画面は本編UI、スマホ撮影復帰、画像生成、AWS同時5人の検証を代替しない。localhost専用で、トンネル公開用の認証設計ではない。

## 実施済み

|検証|結果|
|---|---|
|P0純粋ハーネスのfakeテスト|18件通過|
|P0 HTTP統合テスト|1件通過。分類待ち中の後続訂正受付、古い判定の拒否、Origin/token/接続IDを確認|
|npm test|全88件通過|
|npm run build|型・シナリオ検証・Web/サーバービルド通過|
|Chrome画面確認|1360px/390px。24会話選択、preview時Live無効、横はみ出しなし、JSエラーなし|
|npm run format:check|既存の未変更35ファイルで警告。今回追加したコードの書式は個別確認|

## 未実施・制限

- 日本語の相談→指示・言い直し・途中の間は下記計3セッションを確認。全24会話、後着・英語・任せる指示の成立性と一般的な分類精度は未検証。
- 判定中に発話された音声が判定後に初めて届き、その音声区間をまだ一度も観測していなかったケースは、現在のprobeで意味上の境界を保証しない。既知の判定中区間の後着は拒否する。実測で問題が出たら計画へ差し戻す。
- 固定会話例とfakeテストの成功を、実Live成功として扱わない。P0未完了の間は全面実装ゲートを通過させない。日本語3ケース確認後のユーザーの続行指示に基づき、P1の設定・共有契約のみ先行実装した。
- P4の生成画像検査、P7の実スマホ/AWS同時5人/面白さは未着手。

## 実Live試験 2026-09-13（日本語・相談から実行へ）

ユーザー提供の core-live-probe-ja-execute-1.json と本人の「相談では実行せず、『切って』で実行した」という確認を照合。fixture名はja-execute-1だが、実際には同一接続で相談と指示を順に試した1セッション。全24例の完了とは扱わない。

- 相談はconsult、根拠はserverSeq 11〜16。行動の予約・確定なし。
- 後続の指示はexecute、根拠はserverSeq 30〜32。相談の根拠を再利用していない。
- actionsは1件、committed、success=true。gameVersionは0→1。
- Live接続1回、Responsesは3回（相談分類・指示分類・行動判定）。

|計測|相談|実行指示|
|---|---:|---:|
|根拠の最終delta受信→サーバー委譲受付|1.153秒|1.325秒|
|委譲受付→分類完了|2.150秒|1.729秒|
|行動予約→結果確定|対象外|1.469秒|
|根拠の最終delta受信→結果確定|対象外|4.523秒|

PC上のブラウザ受信時刻とサーバー記録時刻から算出した単一試行。発話終了からの遅延、音声再生完了までの遅延、一般的な応答性能を示す値ではない。

結果のcommentary.append送信は記録されているが、保存時点の出力transcriptは実行の相づちまでで、結果文の音声再生完了はJSONだけから確認できない。Liveの自発的な相づちとアプリの実行通知の重なりも、今後のテンポ調整で確認する。

原本はユーザーのDownloadsに保持し、会話原文をリポジトリへ複製しない。SHA-256: ffd0629424a42bdf3204c30e7a52a3a3845b0d64f62449942fcc5ab73502f4c5。

## 実Live追加試験 2026-09-13（言い直し・途中の間）

ユーザーが両ケースを意図どおりと報告。提供JSONの実際の発言と状態を照合した。両ファイルのfixture欄はja-execute-1のままだが、内容に基づき別ケースとして記録する。

|ケース|観測した動作|判定・行動|
|---|---|---|
|言い直し|切る指示を言いかけた後、「まだ切らないで、切れるかだけ教えて」へ訂正|訂正を含むserverSeq 10〜22からconsult。actionsは0件|
|途中の間|「このハサミを使って」の後に相づちを挟み、2.6秒後に続きの指示|続きが届くまで委譲・予約なし。その後execute、committedは1件|

- 2.6秒はLiveの音声時刻の区間末尾8000ms→次の区間開始10600msの差。現実の無音時間の厳密測定ではない。
- 言い直しの委譲受付→分類は2.675秒。Live 1回、Responses 1回。
- 途中の間の委譲受付→分類は2.308秒、予約→結果確定は2.034秒。Live 1回、Responses 2回。
- 途中の間の認識原文は「ローブを切って」だが、分類は文脈上のロープを対象としていた。原文を書き換えた結果として報告しない。
- 言い直しは分類開始前に訂正文が揃ったケース。推論途中の訂正や判定中発話の未知区間後着を実測したものではない。
- この途中の間では未完時に委譲自体が来ていないため、「未完で委譲が来た後の保留→再評価」が実Liveで通ったとは扱わない。
- 相づち・相談応答は出力transcriptで確認。途中の間ケースの結果音声は送信記録までで、保存時点に再生完了の証拠はない。

原本はローカルに保持し、Gitには集計のみ保存する。

|原本|SHA-256|
|---|---|
|言い直し.json|e0b26e9673ee58e24b01ff6509d9448b586185967d222708ccd03164bc12bef8|
|途中の間.json|e6bd73f9b16cade462533bf6bec1ffa2683cd4e7deee3d4a6c3ca229cd37967e|

## P1 設定・共通契約の検証（2026-09-13）

- npm run build: v1参考設定/v2標準設定/共通JSON検証、型検査、Web/サーバービルド通過。
- npm test: 103件通過。設定・会話構造・カタログ・既存ゲームの回帰を含む。
- 新規HTTPプレイには編集済みJSONを反映し、既存プレイのtitle/digestを維持。破損後も既存開始の再送は成功、新規作成は503 CONFIG_INVALIDでスロットを残さない。
- 256KiB超過・無効JSON・v1の新規本編利用・画像試行枠不足を拒否。deep freeze後の変更を拒否。
- 日英はデータの読込・投影まで。画面の言語選択とLiveの言語切替はP3で接続する。新しい判定/画像policyは本編ハーネスへまだ未接続。
- Dockerfileにconfigコピーを追加したがDocker build/AWS再配置はこの工程では未実施。

## P2 本編統合の検証（2026-09-13）

全140件の自動テスト、npm run build、Chromeでのcore/legacy両smokeが成功。AI・WebRTC・マイクはfakeで、実Liveの追加成功を意味しない。詳細は[tasks/P2.md](tasks/P2.md)。本編の実機では写真送信後の用途質問、相談では回数不変、指示後に1回のみ判定、最後の結果音声を確認する。メッセンジャー履歴・画像演出はまだ後続工程。

## P3〜P6 統合（2026-09-13）

- npm test: 172/172成功。npm run build成功。変更コードに限定したPrettier check成功（リポジトリ全体の既存改行差分は今回の検証と分離）。
- core-result-store 9件、core-result-http 3件、core-scene-http 5件、core-media 15件を含む。
- Chrome: core-smoke（日英、390px/1280px、復帰、画像と会話）とlegacy mobile-smoke成功。実HTTP+Chromeでも開始画像・写真アップロード・認識原文・自動行動1回・合格画像表示を確認。API/マイクはfake。
- 画像未検査時非公開、reject2回でfailed、終了後配送、新旧play分離、drain abortと後着非公開、認証TTL後の読取と保持期限境界を確認。
- 画像モデル利用可否/意味的検査精度、実Live本編のテンポ、実スマホ、AWS5人・実配置は未実施。次の人間試遊はdocs/game-core.mdの順でまとめて行う。

## 導入動画・着信画面（2026-09-13）

ユーザー提供の仮動画と着信SEを追加。認証後「体験を始める」→動画終了/右下Skip→Calling/応答する→マイク許可とLive接続→既存の呼びかけ。動画・着信中はプレイ枠やAI要求を作らない。リプレイ時は言語選択から新しい導入、進行中の再読込は既存プレイへ復帰する。

npm run buildと対象Prettier check成功。Chromeのopening-smoke/core-smoke/mobile-smoke成功。動画の実メタデータ読込、終了/Skip、autoplay拒否時再生案内、着信音停止、応答前のmic/create/Liveゼロを確認。マイクと音声再生はfakeで、実スマホでの音量・音声付きautoplayは未確認。コピーしたMP4/MP3のSHA-256は添付原本と一致。

## 2026-09-13 着信応答から直接プレイ

- coreではLive接続完了のheartbeatで本編と時計を開始し、最初の状況画像を生成する。繰り返しheartbeatでも初期画像は1件。
- 追加の開始ボタンと旧チュートリアルを表示せず、説明から写真送信へ続く。
- conversation.ja/en.openingMessageを導入台詞としてLiveへ渡す。役割は未来の本人ではなく未来の本人を支援するAI。
- npm run build成功、npm test 172件成功。
- Chrome core-smoke / mobile-smoke / opening-smoke成功。自動開始後のopening送信、日英UI、動画・着信、写真送信・再接続を確認。AI・マイクは模擬。実Liveの説明内容・テンポは未確認。

## 2026-09-13 指示分類の実API拒否を修正

- 実APIでcore_intentのdecision.oneOfが400 invalid_json_schemaになることを再現。分類段階で停止するため行動は消費されない。
- Zodの判別共用体はランタイム検証用に残し、送信用は通常のunionからanyOfを生成する。
- 同じ模擬発話の実API要求が修正後200/completed。実写真・実Liveでの通しプレイは未確認。
- oneOf禁止とstrict object/all branchesを検証する回帰テスト追加。npm test 173件成功、npm run build成功。

## 2026-09-13 本編を単一のメッセンジャー画面へ統合

- 通話・残り資源・状況詳細・持ち物・撮影・ライブラリ・送信・終了をアプリ内へ配置。写真プレビューは送信欄へ表示。
- npm run build成功。Chrome core-smoke / mobile-smoke / opening-smoke成功。
- 320×568、390×844、1280×900で画面外の操作欄やページスクロールがなく、会話領域を独立スクロールできることを検証。カメラ/ライブラリアイコンからの選択、プレビュー取り消し、送信・再試行、再接続・終了後表示を模擬API/マイクで確認。
- スマホ幅のプレビューを目視確認。物理端末のカメラ・OS写真選択画面・実Liveでの検証は未実施。

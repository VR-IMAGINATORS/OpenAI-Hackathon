# 本編取り込みの検証記録

2026-09-14。作業ブランチ `codex/story-integration`、独立worktree `artifacts/story-integration`。`560a5b6` を起点に本編側だけを実装した。元フォルダで別AIが進めている動画・リザルト変更は混ぜていない。

## 自動検証

- `npm test`: 219件成功、失敗・skip・todoは0。
- `npm run build`: 成功。内部の `npm run check`（型検査、v1/v2、v3全18構成、共通設定の検証）とWeb・サーバービルドを完了。
- `npm run format:check`: 対象ファイルすべて成功。

| 確認対象 | 検証内容 |
| --- | --- |
| マスター | 元の6舞台・10障害・18構成、日本語の観察・機構・ヒント・成立条件、代表道具の物性との対応 |
| 選出 | 全18候補、言語、開始前の非抽選、HTTP作成リトライ・再読込・操作権移行でのsnapshot保持 |
| 時間 | 300秒を既定に180/450秒の設定変更、新規のみ反映、プレイ枠寿命に収まらない設定の拒否 |
| 進行 | 全10障害の部分進展→完全解除、3解除と最後の行動、success/完了factの不一致・将来障害変更の拒否 |
| 物語と会話 | 日英の導入・初期手掛かりの実際の表示、完全解除数の段階、相談での世界設定、要求時のみ段階ヒント |
| 画像 | 生成入力・検査入力の双方で公開済み障害のみを使用し、舞台設定を維持 |
| 既存構成 | 旧V2を明示指定した全編、音声委譲・写真・道具再利用・認証・HTTP・利用上限の既存回帰 |

外部AIは使用せず、模擬応答と状態・生成入力・HTTP経路を検査した。代表物性のデータ検証や模擬の成功判定は、実AIが代用品を正しく評価した実績ではない。

## 未検証と他担当との統合

- 実GPT-Live音声、実写真、実生成画像の意味、物語の自然さ・面白さ、スマホの試遊は未実施。
- happy/normal/bad/中断の分類、提示済み証拠台帳、EndingPacket、動画→リザルトは別担当のブランチとの統合後に確認する（AC07〜AC09）。本編側で二重実装していない。
- AC03〜AC05・AC10は自動検証部分を確認した。実生成・実機の品質まで合格と扱わない。
- ユーザーの指示に従いマルチレビューは実施していない。push、PR、デプロイは行っていない。

接続箇所と競合し得るファイルは [ending-handoff.md](ending-handoff.md) を参照。そこから参照するending-video文書は別担当の作業ブランチにあり、この独立worktreeには未取り込み。

## 統合後の試遊

### 2026-09-14 ローカル統合の確認

`feature/shibuya/integrate-mock-features` にローカル限定の4コミット
（`f24dbd1`、`a29093f`、`32d3345`、`47c972b`）を統合。
`hosted-runtime.ts` と `apps/server/config.ts` のimport競合は両機能を保持して解消した。

- `npm run check`、`npm run build`: 成功。
- `npm test`: 312件成功、失敗・skipなし。
- 追加した `tests/story-ending-integration.test.ts`: 全18構成×日英×解除数0〜3の144組で、RuntimeからEndingPacketへのsnapshot・初期表示の伏線・解除ID・残る障害・確定行動・facts・結末分類を検証。判断はfake providerを使用。
- `apps/web/tests/core-smoke.cjs`、`ending-smoke.cjs`: Chrome headlessで成功。日英HUD・警告・復元、本編UI、動画待機・手動再生・結果表示・再読込・期限切れを確認。API・メディアは模擬で、実動画のデコードや実音声は未確認。
- 競合解消ファイルと追加テストのPrettier 3.6.2検証、`git diff --check`: 成功。

ブラウザテストはスクリーンショットの出力先のみを `artifacts/integrate-mock-smoke-20260914/` に変更したコピーで実行し、既存の画像を保持した。
未追跡だった `spec.md` と `ending-handoff.md` は `artifacts/integrate-mock-backup-20260914/` に元のバイト列を保持して退避済み。
実AI・実写真・実動画生成・実機試遊、push・デプロイ・マルチレビューは実施していない。

### 実機での手順

1. 統合ブランチで `npm run check`・`npm test`・`npm run build` を実行する。
2. 環境に `SCENARIO_PATH` の明示指定がある場合は `scenarios/story-catalog.json` を指定する。実API設定は既存の開発手順に従う。
3. 日英で新規開始し、選出舞台・最初の拘束・初期手掛かりが音声と画面で一致することを確認する。再接続で舞台が変わらないことを確認する。
4. 世界設定を質問し、現在障害のヒントを順に求める。部分進展では障害・語りの段階が進まず、完全解除後は次の障害が明確になることを確認する。
5. 各結末の入力を動画担当と確認し、初期手掛かり・使用道具・最終状態に沿った結末になり、エンド名・本文・評価が動画後に出ることを確認する。

### 2026-09-14 現在の目標の文言修正

- 原因: 旧V2の標準例だけに目標文を入れ、既定のV3カタログ変換では `gimmick.name` を公開タイトルへ渡していた。Web用カタログの全10障害へ必須の `objective.ja/en` を追加し、変換時の表示元を切り替えた。障害名・元マスター・内部の突破条件や別解は保持した。
- 全18構成×日英×3進行段階で、模擬の行動確定後に公開状態へ正しい目標文が渡ることを検証。目標文の欠落・片言語欠落・空文を設定検証で拒否する。元の10障害の意味・移植元との一致を確認する既存テストも成功。
- `npm run check`: 成功。`npm test`: 319件成功、失敗・skipなし。Viteとサーバーのビルドも成功。既存出力を上書きせず `artifacts/objective-copy-20260914/web` と `server` に出力した。
- Chrome `core-smoke.cjs`: 既定カタログのロープ→曇った窓の構成を使い、日英ラベル、目標の切り替え、320px/390px/1280px表示、詳細開閉、警告閾値・再読み込みを確認。画面は `artifacts/objective-copy-20260914/` に保存し、既存の画像を保持した。
- 横断確認: カタログの必須フィールド → 実行用シナリオの `obstacle.title` → `GameSession.state()` → `PlayScreen` / `GameStatusSummary`、仕様、プランナーガイド、開発ガイド、実行時・ブラウザテストを確認。明示指定用の旧V2は前回修正済みの目標文を維持する。
- API・音声・マイクは模擬。実AI、物理スマホ、公開環境は未検証。反映には修正版サーバーで新しいプレイを開始する。進行中のsnapshotは変更しない。

# 検証記録（2026-09-15）

## 成功

- `npm.cmd run check`: TypeScript、標準シナリオv1/v2、18構成のv3カタログ、ゲーム設定。
- `npm.cmd test`: 作業フォルダ全体566件成功。偽AI/HTTPによる難易度固定、枚数消費、会話の断片・再送・委譲救済、訂正無料、失敗返却、残高不足、再接続、枯渇、最後の写真の勝利、時間切れと従来の結果評価を含む。
- Proで短い会話50往復を完了して0になり、低残高警告1回→指定文で終了する回帰テスト。
- `npm.cmd run build`: 型・JSON確認、Vite、統合サーバーのビルド。
- Chrome headlessの opening/core/mobile スモーク。日英のプラン名、320/390/1280px、本編の料金・残高・短い消費表示、20%警告、写真2枚200の可否、返却後の残高増加、0で処理完了待ち→既存結果画面を確認。スクリーンショットを目視。
- `git diff --check`。
- クレジット実装だけを選択した一時インデックスから確認用コピーを作成し、型・JSON・全566テスト成功。並行作業の未コミット部分への依存がないことも検証。

## 実行環境と範囲

PowerShellのnpm.ps1実行制限があるためnpm.cmdを使用。キャッシュ内のPrettierとPlaywright/Chromeでローカル検証。成果物は `artifacts/game-credits-20260915/` と `artifacts/credit-*.txt`（Git対象外）。

並行作業による導入表示・時間警告の差分が同じフォルダに存在する。全566件はそれらを含む作業フォルダの検証であり、今回のコミットはクレジット関連だけを選択する。

実OpenAI API・GPT-Live音声の分類精度・実スマホ撮影・実際のゲームバランスは未検証。文字起こし断片には確定turnIdがないため、通常の会話はclient委譲後の意味分類で精算する。実音声が委譲指示に従うかは試遊で確認する。

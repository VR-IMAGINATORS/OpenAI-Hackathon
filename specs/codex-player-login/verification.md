# 検証記録 — 2026-09-18

- TypeScript型チェック、シナリオ検証、Viteビルド、サーバービルド: 成功。
- 全体テスト: 993件成功。以降はテストのみ強化し、新機能の6件を再実行して成功（モデル非対応時の認証破棄追加と、HTTP→AiService→所有者別workerの経路確認）。
- ブラウザsmoke: Chrome headless、模擬認証。未認証の開始制限、承認待ち表示とリンク、再読込復元、ready後の開始許可、ログアウト、失敗後の再試行、キャンセル、日本語、390px幅、通常API版でログインUI非表示を確認。ページ例外なし。
- スクリーンショットを目視確認: `runs/codex-login-smoke/pending-mobile.png`（認証コードはテスト用）。横方向のはみ出しなし。
- `git diff --check`: 成功。

自動認証テストはfake workerを使用。2所有者の分離、playId紐付け、未知play拒否、プレイ中のアカウント変更拒否、終了後の認証破棄、容量、待機/readyの期限、遅延startupの取消、失敗時の非公開化を確認。HTTPではCookie/Origin/bodyの制限も検証した。

ユーザーが新しいゲーム画面から実OpenAIログインに成功したことを報告（2026-09-18）。新しい画面認証版での実推論・ゲーム完走までを今回の報告から確認済みとは扱わない。旧端末認証版の写真→ギミッククリア（9801ms）とは区別する。公開運用/スマホ実機/別人の実アカウント2個による認証分離は未検証。

## 再プレイ時の認証保持（2026-09-18）

従来の終了後破棄を変更。正常終了後10分間、同じCookie所有者の認証を再利用する。旧playIdは拒否し、処理のキャンセルと終了・音声停止を待ってから次のプレイへ進む。ログアウト、保持期限、サーバー終了、worker失効時は破棄する。

型検査、ログイン/音声の関連16テスト、UIとサーバービルドに成功。HTTP再プレイで認証workerが増えないこと、別所有者の隔離、旧処理中の再bind拒否、期限更新と破棄、音声の新規セッション作成を模擬検証した。実アカウントでの再プレイは未確認。

ユーザー実機確認: 再プレイ時にログアウトされず、認証を保持できることを確認（2026-09-18）。

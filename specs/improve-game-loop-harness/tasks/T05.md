# T05: 写真自律実行と自然な会話の統合

進捗記録用。GitHub issueと同じ受け入れ条件。詳細な型・APIは ../data-model.md と ../plan.md を参照。

- 計画: §3 P3
- 依存: T03,T04
- 対象: apps/local-server/hosted-runtime.ts、game-ai.ts、companion-response.ts、live.ts、story.ts、config/game-core.json

写真認識を共通判断へ接続し、公開済み情報だけで結果と状況を説明する。

完了条件:

- [ ] 自明なら用途質問なし、曖昧なら質問、危険なら必要な確認。一つの障害を越えたら判断をユーザーへ返す。
- [ ] 秘密を知る判定器の自由文をLiveへ流さず、メタ発言・不要な回数通知を抑える。確定後だけ画像を生成し、日英の導入と復旧文も整合させる。
- [ ] 実装・模擬テスト・実AI・実機の確認を区別して記録する。



## 2026-09-15 公開結果・発話生成の担当記録

- `game-ai.ts`: core判定器が返すnarrative/situation/shortReason/在庫descriptionを破棄し、成立候補のautomatic observable定義と短い公開成否文へ投影。fact/inventory差分の最終検証はGameSession。snapshotなしlegacy判定は維持。
- core写真認識の入力から障害goal/constraints/宣言済み将来状態を除外し、認識用公開状態へ限定。
- `companion-response.ts`: 明示的な公開フィールドだけを使う短い結果発話。秘密の追加プロパティを転送しない。ナレーション失敗時は確定済み公開文へfallbackし、行動を再実行しない。
- 模擬検証: 新規`tests/harness-companion-response.test.ts`6件とcore-game16件、22/22成功。秘密canaryの除外、認識入力境界、返却前投影、発話失敗fallbackを確認。
- runtime統合・実AIの表現精度・実音声の自然さは別途確認。T05全体の完了を意味しない。

## 統合模擬adapter移行と残課題

- core-runtime/core-full-gameのmockをschema名で分岐し、写真分類を音声分類に誤認しない形へ更新。新ナレーション・知識選択APIに対応。pollのserverNowだけは再読取時に進むことを検証する。
- story-runtimeの既存架空シナリオfixtureに明示的な既知/観察/条件付きヒントを定義し、従来の段階ヒント確認を維持。15/15成功。
- core-runtimeへ写真のみの一度実行、危険確認、拒否、実行中取消し、同文音声重複の5件を追加。前3件成功。後2件は既定AI_RESPONSE_CONCURRENT_PER_PLAY=1が制御分類を拒否する実装課題を検出し、統合担当へ報告した。テストの上限引き上げで隠していない。
- 調査時のcore/story群157件154成功3失敗・timeoutゼロ。旧HTTP1件はmockとserverNow対応後に個別成功。制御並列枠の2件は設定/実装修正後の再確認が必要。


## 統合状況（2026-09-15）

実装と決定的な模擬検証を統合済み。実音声の自然さ・モデル判断の品質はT08で確認する。最終実行結果は../verification.mdを参照。

- H10追加統合確認: 拒否済み写真への同じ押し切りは拒否を維持し、新しい物性情報が入った場合だけ再評価で実行へ進む。再評価inputのpriorDecision/userSpeech/公開stateを検証、再認識・送信再消費なし。core-runtime20/20成功。実モデルの物性判断精度はこの模擬確認と区別する。

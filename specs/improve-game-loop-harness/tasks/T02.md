# T02: ハーネス契約と設定・シナリオ移行

進捗記録用。GitHub issueと同じ受け入れ条件。詳細な型・APIは ../data-model.md と ../plan.md を参照。

- 計画: §3 P1
- 依存: なし
- 対象: packages/shared/harness.ts、conversation.ts、core-config.ts、scenario.ts、story-catalog.ts、apps/server/scenario-catalog.ts、config/game-core.json、scenarios/story-catalog.json

実行根拠・知識・警告の契約と新規プレイに固定する設定を整備する。

完了条件:

- [x] 旧設定の明示的な正規化と全18構成の参照整合を検証する。
- [x] 自然文の許容方針と日英設定を編集でき、秘密を公開stateへ追加しない。型はdata-model.mdを参照し独自に補完しない。
- [x] 実装・模擬テスト・実AI・実機の確認を区別して記録する。



## 実装記録 (2026-09-15)

harness契約、共通設定schemaVersion 2、旧単一警告の明示正規化、既存カタログ知識・全18構成検証を実装。ScenarioCatalogの既存parse→digest→deep freeze経路で正規化した設定を新規プレイごとに固定するため、このクラスの追加変更は不要。公開projectionにはknowledgeを追加していない。

検証: tests/harness-knowledge.test.ts + core-config.test.ts + story-catalog.test.ts の16テスト成功（課金なし）。npm run checkは初回成功、後続時点は他担当のruntime統合中メソッド不足による失敗のみ。最終全体確認は親担当。実AI・実音声・PC/スマホ確認は未実施、模擬テストの成功と区別する。
## 追加: 道具を使わない環境操作の契約 (2026-09-15)

ExecuteIntentへ後方互換のmode/tool・environmentTargetIdsを追加。toolは非空itemRefs、environmentは非空の一意な対象IDと空itemRefsを要求。写真起点は元写真参照を含むtoolに限定する。意図判断へ現在障害の宣言済observationTargetのみを渡し、予約時にも同じ範囲を検証する。見る・聞くだけは開示/相談のまま、状態変更は通常のjudge/commit検証を通す。Proposal型でサーバー検証済みmode/対象IDを判定へ保持し、認識providerのschemaには権限フィールドを追加しない。RiskProposalも同じ対象契約を維持する。

模擬検証: harness-actions11件（新規環境操作2件）、harness-knowledge10件（意図入力の現在対象限定を含む）成功。npm run check成功。実音声・実モデルの物理判断は未検証。


## 統合状況（2026-09-15）

実装と決定的な模擬検証を統合済み。実音声の自然さ・モデル判断の品質はT08で確認する。最終実行結果は../verification.mdを参照。

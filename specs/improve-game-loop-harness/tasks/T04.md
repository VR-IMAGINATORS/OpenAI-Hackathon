# T04: 自律行動と確定前訂正・再試行

進捗記録用。GitHub issueと同じ受け入れ条件。詳細な型・APIは ../data-model.md と ../plan.md を参照。

- 計画: §3 P2
- 依存: T02
- 対象: apps/local-server/action-coordinator.ts、intent-coordinator.ts、conversation.ts、core-intent-ai.ts、game.ts

写真・音声の共通行動管理と、判定中も動く訂正制御を実装する。

完了条件:

- [ ] 予約成功と根拠予約を同期させ、古い結果とfinallyを無効化する。
- [ ] 同一operationの再試行は追加一度、具現化IDを維持。結果確定前の訂正、写真と音声の重複、確定後通知失敗を競合テストで確認する。
- [ ] 実装・模擬テスト・実AI・実機の確認を区別して記録する。



## 2026-09-15 GameSession担当の実装記録

- `game.ts`: 同期commitActionResultと判定の分離、pendingActionId/holdPendingAction/resolvePendingActionControl/cancelPendingActionを追加。受信済み制御入力の未解決中は確定を待ち、5秒未解決なら取消。古いfinallyは次のticketの時計を再開しない。
- 技術的再試行はUPSTREAM_FAILEDの5xxだけ追加一度。同じticket・写真context・具現化IDを保持し、不正schema/fact・利用上限・ゲーム上の失敗は再試行しない。既存ai-service.respondに内部再試行なしを確認。
- 写真originは現在の受信写真集合・認識・inputRevision一致を予約時に検証。requestIdの取込重複管理、音声根拠予約、runtimeの制御配送は別の統合担当。
- 模擬検証: `node --import tsx --test tests/harness-actions.test.ts tests/core-game.test.ts` 25/25成功（新規9件）。取消し順序、保留解除、5秒timeout、旧finally、再試行上限、写真根拠を確認。`tsc --noEmit`成功。
- 実AI・実Live・PC/スマホの確認は未実施。T04全体の完了はcoordinator/runtime統合と重複根拠テスト後に判定する。

## 制御用通信枠と判定取消しの追記

- AiServiceに内部専用respondControl(playId, body, signal?)を追加。通常response同時上限は変更せず、制御分類harness_controlのみ別に同時1件。play/globalの試行予算とglobal同時上限は通常・制御の合算を維持。HTTP bodyのlane指定は拒否。
- AbortSignalをcontrolおよびGameSession→GameAI→AIResponsesClient→AiServiceの判定経路へ伝播。取消し・世代失効で判定をabortし、古いticketはACTION_INVALID扱い。実上流がまだ終了していなければbusy予約を維持する。
- 追加模擬テスト: 専用枠、通常/global上限維持、試行予算合算、body経路選択拒否、協調abort/非協調上流保持、判定signal伝播。hosted-ai/harness-actions/harness-companion-response合計37/37成功。
- runtime統合後のcore-runtime18/18成功。以前記録した制御分類のREQUEST_LIMITによる2件の失敗は専用枠導入で解消。実AI/実音声は未検証。

- 追加統合確認: cooperative abort→実ユーザー訂正fragmentの再委譲→新しい判定だけ1件確定。旧判定のAbortSignal受信、旧結果/画像なし、写真再消費なし、実delegation IDで結果通知を検証。core-runtime19/19成功、tsc --noEmit成功。


## 統合状況（2026-09-15）

実装と決定的な模擬検証を統合済み。実音声の自然さ・モデル判断の品質はT08で確認する。最終実行結果は../verification.mdを参照。

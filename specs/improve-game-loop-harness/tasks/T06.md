# T06: 音声活動APIと期限付き警告配送

進捗記録用。GitHub issueと同じ受け入れ条件。詳細な型・APIは ../data-model.md と ../plan.md を参照。

- 計画: §3 P4
- 依存: T01,T02
- 対象: apps/server/app.ts、apps/web/src/PlayScreen.tsx、live.ts、apps/local-server/voice-notifications.ts、live-outbox.ts、hosted-runtime.ts

通常警告を会話の切れ目へ送り、最終警告は有限待機で通知する。

完了条件:

- [ ] 60秒通常・15秒最終・最大3秒待機を初期設定にし、unknown/staleを静寂と扱わない。
- [ ] 専用APIの所有権・rate limit・sequence、outbox期限とackを検証。終了後警告を棄却し、結果通知を失わず、最終警告の聴感を実機で確認する。
- [ ] 実装・模擬テスト・実AI・実機の確認を区別して記録する。



## 2026-09-15 実装進捗

- [x] VoiceNotificationScheduler、専用API route、UIの独立テレメトリー経路を実装。
- [x] outbox serverNow/警告validUntil・期限切れack・通常から最終への置換を実装。
- [x] scheduler/outbox/monitor計13件の模擬テスト成功。
- [x] 親担当のGameRuntime接続後、HTTP統合を含む22件とTypeScript検査成功。OpenAPIのCommand/CommandBatchにもmetadataを反映。
- [ ] 最終警告の聴感・PC/スマホ実機確認。T01実機ゲート未達を継承し、完了扱いにしない。

- [x] 同時待ちの警告と結果を一件のpayloadへ結合するcallbackを追加。scheduler10件成功。結合は期限内だけ・確定結果に期限を付けない。runtime callbackも接続し、最終全体テスト成功。実聴感は未確認。
